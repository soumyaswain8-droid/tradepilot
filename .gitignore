# Python
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store
Thumbs.db

# Data (large, regeneratable)
prototype/data/*.csv
prototype/data/cache/
prototype/data/intraday/

# Rust build artifacts
engine/target/

# Claude Code session memory — ignore everything inside .claude/ EXCEPT team charter
.claude/*
!.claude/team/

# Ephemeral team logs (Sprint 1 audit data) — never commit
docs/team/status/*.json
docs/team/activity/*.jsonl
docs/team/audit/*.jsonl
docs/sarathi/ledger/*.jsonl
docs/slippage/*.jsonl
docs/exec/*.json

# Models — now tracked in git (repo is private)
# prototype/models/*.pkl

# Temp
*.log
.env

# ═══ SECURITY — Never commit these ═══
# Telegram bot credentials
prototype/v5/telegram_config.json

# Analytics DB (contains user data)
prototype/tradepilot_analytics.db
*.db

# Paper trade data (trade secrets)
# docs/paper-trades/ — temporarily tracked (will move to Rust DB later)
logs/

# Model weights — now tracked (repo is private)
# prototype/models/*.pkl  (removed from gitignore — backed up in git)
# prototype/models/*.json (removed from gitignore — backed up in git)
prototype/models/*.h5

# Deployment artifacts
deployments/

# Secrets
*.pem
*.key
credentials.json

# Quant data caches (regenerable: bhavcopy, EOD, panels) — large, not source
quant/data/

# Playwright MCP test-tool artifacts (local junk, never commit)
.playwright-mcp/
