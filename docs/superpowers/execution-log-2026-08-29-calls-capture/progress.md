# SDD ledger — plan: docs/superpowers/plans/2026-08-28-calls-capture-pipeline.md
Spec: docs/superpowers/specs/2026-08-28-client-dashboard-design.md (read; binding authority)
Worktree: .worktrees/calls-capture on feat/calls-capture. BASE = 272622e.

PRE-FLIGHT CONFLICT SCAN
Cross-task interface pairs (only app_store crosses task boundaries; Tasks 2/3/4 share no interface with each other):
| pair | produced | consumed | finding |
| T1 -> T2 | get_db(path=None), init_db(conn), DB_PATH | T2 tests call get_db(str(tmp)) positionally; main() calls get_db() bare | OK — optional arg satisfies both |
| T1 -> T2 | calls columns: id,symbol,side,published_at,price_at_call,score,signal,horizon,target,stop | T2 insert_rows names exactly those ten | OK — exact match |
| T1 -> T3 | calls columns outcome_price,outcome,outcome_at + SELECT * rows | T3 apply_outcome UPDATEs those three; due_calls reads horizon,side,price_at_call,target | OK — all present in schema |
| T1 -> T4 | calls columns published_at,outcome | T4 summarise reads only those two | OK |
| T1 -> T2/T3/T4 | ux_calls_symbol_day unique index | T2 relies on IntegrityError to skip duplicates | OK — PK and index both collide; either raises IntegrityError |
Per-task self-consistency (tests specified vs code specified):
| task | finding |
| T1 | 7 tests named, plan asserts 7. FK test needs PRAGMA foreign_keys=ON, which get_db sets. OK |
| T2 | 10 tests named, plan asserts 10. build_rows(payload, published_at) signature matches every call site in its own tests. OK |
| T3 | 13 tests named, plan asserts 13 (after preflight fix). classify/4 used consistently in all 6 call sites. OK |
| T4 | 5 tests named, plan asserts 5. Date arithmetic VERIFIED: 2026-08-26 Wed, 08-27 Thu (the asserted gap), 08-28 Fri, 08-29 Sat + 08-30 Sun (the asserted weekend), 08-31 Mon. OK |
| totals | 184 + 7 + 10 + 13 + 5 = 219, matches the plan's stated final count. OK |
Preflight finding 1: the `_load()` importlib helper appears verbatim in three test files (T2, T3, T4) — the review rubric treats verbatim duplication of a logic block as a defect, and the plan mandates it.
Ruling: keep the duplication; do NOT extract it to a shared conftest helper. — Why: each test file must import a DIFFERENT hyphenated script, the helper is 6 lines of pure scaffolding with no logic to get wrong, and a shared helper would couple three otherwise independent test files. Test files earn standalone readability more than they earn DRY. — Cost if wrong: six duplicated lines to consolidate later, no behavioural risk.
Task 1: implementer DONE (commit b25c764, 184 -> 191 tests). Verified independently: all three pragmas set, ux_calls_symbol_day present, FK present, only app_store.py + its test touched, analytics DB untouched.
Note: review-package run from the worktree writes its diff into the WORKTREE's .superpowers, not this workspace. Harmless (absolute paths), but run it from the main checkout for tasks 2-4 so briefs, reports and diffs share one directory.
Task 1: review — Spec ✅, quality approved. 0 Critical, 0 Important, 3 Minor.
Task 1: reviewer limitation noted — the code-reviewer agent type has no Bash, so it could not execute the one check that mattered most. Its static reasoning was sound but I verified empirically myself. For later tasks, either accept static review or use a general-purpose reviewer.
Task 1: minor (deferred): [M1] get_db uses `path or DB_PATH`, so an empty-string path silently falls back to the production DB instead of erroring. Not reachable from any current caller.
Task 1: minor (deferred): [M2] no test asserts os.path.isabs(DB_PATH), though the interface promises absoluteness.
Task 1: CARRIED FORWARD (not deferred) — [M3] the idempotency index binds only for well-formed timestamps. I verified empirically: '2026-08-28T09:20:00' -> date() = '2026-08-28' and duplicates are REJECTED; a malformed '28-08-2026 09:20' -> date() = NULL, and because SQLite treats NULLs in a UNIQUE index as distinct, TWO rows were accepted with no error. Task 2 must write datetime.now().isoformat(timespec="seconds") exactly. Carried into the Task 2 dispatch and its review.
Task 1: complete (commits 272622e..b25c764, review clean)
Task 2: implementer STALLED mid-task — launched a background command and stopped waiting for a notification that never reached it. Not one of the four statuses; treated as an incomplete task, not a fix round.
Task 2: state at stall (verified by me): both files created but UNCOMMITTED, suite already 201 passing (191 + 10), a scratch file scripts/_run_app_5051.py left untracked, and its own server still alive on port 5051 (pids 15040, 15293). No commits.
Task 2: resumed the same implementer with the blocker cleared — told it the server is already up, to run everything in the FOREGROUND, to delete the scratch file, to kill only its own server, and to record the exact published_at value written.
Task 2: implementer DONE (commit 83f1686, 191 -> 201). Live double-run proved idempotency: 10 inserted then 0. published_at format correct ('2026-08-29T13:21:49').
Task 2: CRITICAL defect found by ME during verification, not by the tests. The plan's build_rows mapping was written against score_stocks_v4()'s INTERNAL record, but /api/picks transforms it before serving. Real payload: direction='BUY' (not 'UP'/'DOWN'), target=3.2 (a PERCENT, key `target` not `target_pct`), stopLoss=1.3 (percent, camelCase, not `stop_loss_pct`), reasons=list of DICTS {text,type} (not strings). Consequences in the 10 rows actually written: every BUY recorded as side='SELL'; target and stop NULL for all 10; signal stored as Python dict reprs.
Task 2: root cause — all 10 tests passed because the PAYLOAD fixture I wrote into the plan encoded the SAME wrong assumption as the code it tests. A shared incorrect premise between fixture and implementation is invisible to unit tests. Only the live run against the real endpoint exposed it.
Task 2: Ruling — this is a plan defect, not an implementer defect, and the spec is the binding authority ("record what was published", "signal = plain-English reason", "a hit requires reaching the published target"). Fix the mapping against the REAL payload rather than deferring. Also: record ONLY picks whose direction is BUY or SELL. The scorer emits BUY/HOLD/AVOID (visible in its own log line "BUY=35 HOLD=84 AVOID=81"), and a HOLD is not a call — recording one and later grading it would manufacture a hit rate out of non-advice. — Cost if wrong: fewer rows captured per day than the count parameter suggests, and the fix is a one-line predicate change.
Task 2: Ruling — the 10 rows already written to the worktree DB are wrong-sided and must be deleted as part of the fix, not left to pollute the first day of the record.
Task 2: fix round 1/5 (4 addressed, 0 open — side/HOLD-AVOID/target-stop/signal; commits 83f1686..16453da, 205 tests). Verified live: sides correct, stop<price<target on all 10 rows, arithmetic exact to the paisa, zero dict reprs.
Task 2: re-review raised a NEW latent issue, not a regression. It traced composite_scorer.py:138-145 and found target/stopLoss are UNSIGNED positive magnitudes independent of direction, and that the engine only ever emits BUY/HOLD/AVOID — never SELL. The SELL branch is therefore unreachable today, but my F2 ruling explicitly lets SELL through, and the arithmetic is unconditional: target = price*(1+pct/100) regardless of side.
Task 2: Ruling — fix it rather than park it. The new SELL test passes only because its fixture INVENTS negative percentages (target: -2.0), a convention that exists nowhere in this codebase. That is the identical failure mode that caused the round-1 defect: a fixture asserting a shape reality does not have, passing because the code agrees with the fixture. Left alone, a future SELL would get a target ABOVE its entry, and classify() would grade a short as a hit whenever the stock rose. — Cost if wrong: one small round spent on a branch that is currently unreachable.
Task 2: fix round 2/5 (1 addressed, 0 open — SELL sign; commits 16453da..d9f3a29, 206 tests). Verified by driving build_rows directly: BUY 990<1000<1030, SELL 970<1000<1010, and negative inputs cannot flip it (abs guard). Re-review confirmed the mirror test binds — under a hardcoded sign=1.0 the SELL row computes 1030/990 and both assertions fail.
Task 2: complete (commits b25c764..d9f3a29, review clean after 2 fix rounds)
Task 3: carried forward — /api/stock/<symbol> returns key `price` (VERIFIED live earlier: CIPLA -> 1418.7), so the resolver's fetch_price assumption holds. Unlike Task 2's picks payload, this one was checked before dispatch.
Task 3: carried forward — the DB now holds 10 REAL rows (all BUY, real targets, horizon 'intraday'), so the resolver can be exercised against real data rather than only fixtures. Given Task 2's failure was a fixture that agreed with wrong code, the implementer is told to check its fixtures against those real rows.
Task 3: implementer DONE first pass, no fix rounds (commit d1cd47d, 206 -> 219). The carried-forward Task 2 lesson worked: implementer checked its fixtures against the 10 real rows before trusting them and reported they matched on first check.
Task 3: verified by me against the REAL rows, not fixtures — boundary exact (0 due at +19h, 10 due at +1d 1s); strict target rule holds (exit 1019.9 vs target 1020.0 -> miss, 1025.0 -> hit); SELL direction correct (975 vs target 980 -> hit, 985 -> miss); no-target fallback rose->hit / flat->miss; main() skips a None price rather than classifying it.
Task 3: review — Spec ✅, quality approved. 0 Critical, 2 Important (both labelled brief/plan-level), 1 Minor. Reviewer confirmed atomicity (single UPDATE, no hit-with-NULL-price path), re-entrancy (due_calls filters outcome='open', no retroactive flips), and that all three key tests genuinely bind. Explicitly found NO fixture/implementation collusion this time, unlike Task 2.
Task 3: Ruling on Important 1 (horizon fallback) — FIX, do not park, and change the plan-mandated test that asserts the unsafe direction. resolve-calls falls back to the SHORTEST horizon (intraday, 1 day) for any unrecognised value, and publish-calls writes horizon straight from the payload with no whitelist. So a horizon added to the scorer later but not mirrored into HORIZON_DAYS silently grades an 'investment' call 29 days early while main() reports success. That is a silent violation of the spec's central invariant ("a call inside its horizon must NEVER be counted"), and the fallback errs in the one direction it must not. Falling back to the LONGEST known horizon cannot produce that failure. The spec outranks the plan, so test_unknown_horizon_falls_back_to_intraday is rewritten. — Cost if wrong: an unrecognised horizon resolves late or never rather than early, which shows up as calls stuck 'open' in calls-status — visible, not silent.
Task 3: Ruling on Important 2 (fetch_price aborts the batch) — FIX. A raise from urlopen or float() escapes the per-row loop into main()'s blanket except, so one flaky quote leaves the other 9 due calls unattempted for a whole cycle. Inconsistent with the same script's graceful per-row handling of a well-formed-but-missing price. Not a correctness bug (unresolved calls stay 'open' and are never counted) but it is exactly the "one bad symbol blocks the whole day" failure the plan warns against. — Cost if wrong: a genuinely broken quote endpoint now produces 10 per-symbol warnings instead of 1 aborted run; the non-zero exit is preserved either way.
Task 3: minor (deferred): [M4] is_elapsed/due_calls are documented pure but have no format contract on `now`; a naive/aware mismatch would raise TypeError. Not reachable through main() today (both scripts use naive datetime.now().isoformat(timespec="seconds")). Fails loudly if ever reached, so a docstring note suffices.
Task 3: fix round 1/5 (2 addressed, 0 open — horizon fallback direction, per-row fetch failures; commits d1cd47d..257f73e, 220 tests). Verified myself: unknown horizon False at +1d and +7d, True at +30d; _FALLBACK_DAYS genuinely derived via max(HORIZON_DAYS.values()) so a future longer horizon moves it automatically; known horizons still boundary-exact at 1/7/30; real rows still 0 today / 10 at +2d.
Task 3: re-review traced the new batch-resilience test in detail and found it NOT neutered — it patches resolve_calls.fetch_price (a real module global main() looks up at call time), never main itself, and reads the result from a third independent connection to the on-disk file. The implementer's two self-reported snags were lifecycle fixes, not assertion weakening.
Task 3: minor (deferred): [M5] the new batch test is the only test in the file that calls main() without an injected clock, so it leans on the real wall clock being at or past 2026-08-29. It only becomes more true as time passes, so it cannot start failing, but it is a style break from every other test in the file.
Task 3: complete (commits d9f3a29..257f73e, review clean after 1 fix round)
Task 4: Ruling BEFORE dispatch — the plan's Step 6 writes two plists into ~/Library/LaunchAgents and runs `launchctl load`, which is a side effect OUTSIDE the worktree that would schedule recurring jobs on the user's machine. I am not delegating that. Task 4 will instead write both plists into the repo at deploy/launchd/ as tracked artifacts, and docs/CALLS_PIPELINE.md will carry the two-line install command. Installation is surfaced to the user at the finish as their decision. — Why: the four stop conditions include side effects outside the worktree that norms say to ask about first, and scheduling daily background jobs on someone's laptop is squarely one. Writing the artifacts keeps the deliverable complete without taking the action. — Cost if wrong: the user runs two `launchctl load` lines themselves, which they would want to do knowingly anyway since the jobs need the Flask app running to succeed.
Task 4: implementer DONE (commit 0d37551, 220 -> 225). Ruling followed exactly: nothing written to ~/Library/LaunchAgents, no launchctl run, plists tracked under deploy/launchd/ with the /Users/YOURNAME placeholder intact. Runtime DB confirmed gitignored via *.db and untracked.
Task 4: implementer surfaced the Saturday anomaly itself, unprompted, rather than reporting a clean exit 0 and moving on. That was the most valuable thing it did.
Task 4: review — Spec ✅, task quality CHANGES NEEDED. 1 Critical, 4 Important, 3 Minor. All inherited from the brief's reference code, none implementer-introduced.
Task 4: Ruling on the Critical (empty-DB blind spot) — FIX. With zero rows, `days` is empty, the gap loop is skipped, and the script reports `gaps none` / exit 0. Unlike the dead-pipeline case (which self-corrects by the next weekday as gaps accumulate), this state has NO time bound: first_call stays None forever, so a pipeline that has never run once is indistinguishable from one running perfectly — from the sole alerting mechanism. The failure it exists to catch is the one it cannot see. — Cost if wrong: a fresh install prints a loud "no calls ever recorded" line and exits 1 until the first successful run, which is the correct default for a tool whose job is turning silence into signal.
Task 4: Ruling on the "(N trading days)" mislabel — FIX by renaming the label, not by changing the computation. days_covered counts distinct calendar days that have rows, with no weekday filter, and the live output literally calls a Saturday "1 trading days". Renaming to "days with calls" makes the label match what is computed; weekday-filtering the count would instead change a number other things may later read. — Cost if wrong: a cosmetic string, trivially revertible.
Task 4: fix round 1/5 (5 addressed pending re-review — empty-DB blind spot, trading-days mislabel, main() exit-code tests, docs caveat, docstrings; commits 0d37551..4bd948c, 227 tests).
Task 4: verified all three branches myself by driving main() against throwaway databases — EMPTY -> "NO CALLS EVER RECORDED" exit 1; Wed+Fri with Thursday absent -> "MISSING DAYS 1: 2026-08-27" exit 1; three consecutive weekdays -> "gaps none" exit 0. Real 10-row DB correctly does NOT take the empty branch and now prints "1 day with calls".
Task 4: note — my own first verification script hit the exact self-patching RecursionError the Task 3 implementer had reported (replacing app_store.get_db with a lambda that calls app_store.get_db). Captured the original first and it was fine. Worth remembering: that trap is easy to fall into and, if "fixed" by weakening an assertion, silently guts the test.
Task 4: re-review — all 5 addressed, no new breakage. Confirmed both new tests assert rc == 1 directly (not stdout as a proxy), and that their monkeypatch closes over the fixture connection by value so the recursion trap never applies. summarise()'s return contract byte-for-byte unchanged.
Task 4: complete (commits 257f73e..4bd948c, review clean after 1 fix round)
ALL FOUR TASKS COMPLETE. Branch 272622e..4bd948c, 8 commits, 184 -> 227 tests.
FINAL whole-branch review (opus, 8 commits, 272622e..4bd948c): CHANGES NEEDED. 1 Critical, 7 Important, 9 Minor. Deferred list triaged: [M1] fine-to-defer (fold in if a wave happens anyway), [M2] actually-not-a-problem, [M4] fine-to-defer but its real containment is I3, [M5] fine-to-defer.
FINAL C1 CONFIRMED EMPIRICALLY BY ME: v4 composite_scorer emits "stopLoss"/"target"; v2 trading_engine and v1 ai_scorer emit "stop_loss_pct"/"target_pct". build_rows reads only the v4 spelling, so when HAS_V4 is false — an ordinary runtime condition app.py already prints a message for — EVERY row is captured with target=None, and classify silently switches to "any rise is a hit". Proven: the same +0.5% move grades `miss` against a real target and `hit` against None, pooled into one published percentage. The original plan used target_pct (right for v2, wrong for v4); my fix round 1 used target/stopLoss (right for v4, wrong for v2). Neither ever handled both.
FINAL Ruling — fix C1, I1, I2, I3, I5 in ONE fix wave. Do NOT fix I4 (holiday calendar), I6 (entry priced by v4, exit by v2), I7 (record location) — the reviewer itself flagged those three as judgement calls for the user, and each needs a decision I should not take on their behalf.
FINAL Ruling on C1's second half — do not "record which rule graded it", ELIMINATE the second rule. A call with no published target cannot be graded against a target, so it must be excluded from the hit rate rather than graded by a softer rule. New outcome value 'ungraded'; calls-status excludes it from the denominator and reports the count. — Why: two rules behind one percentage is indefensible however it is labelled, and an excluded call is honest where a leniently-graded one is not. — Cost if wrong: a handful of target-less calls sit ungraded and visible instead of inflating the rate.
FINAL fix wave: commit 91f8e48, 227 -> 234 tests. All five verified by me: both engine spellings yield identical levels with v4 preferred; classify raises on a None target so no soft rule remains; an error payload raises; an overdue open call alerts with exit 1; docs and resolver wording now match the method; the 10 Saturday dev rows deleted.
FINAL re-review: READY TO MERGE. All 5 addressed, no new breakage. Traced the ungraded path across all three scripts and found no route by which an ungraded row reaches the hit-rate numerator or denominator; confirmed main()'s None-target guard runs strictly before classify so the new ValueError cannot surface as an aborted run.
PLAN COMPLETE: 9 commits, 272622e..91f8e48. Tests 184 -> 234.
