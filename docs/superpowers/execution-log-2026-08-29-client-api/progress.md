# SDD ledger — plan: docs/superpowers/plans/2026-08-29-client-api-layer.md
Spec: docs/superpowers/specs/2026-08-28-client-dashboard-design.md (read; binding authority)
Worktree: .worktrees/client-api on feat/client-api. BASE = 717a7d7. Baseline 234 tests.

PRE-FLIGHT CONFLICT SCAN
Cross-task interface pairs:
| pair | produced | consumed | finding |
| T1 -> T2/T3/T4 | client_api.bp (Blueprint, url_prefix=/api/app) | all three add routes to it | OK |
| T1 -> T2/T3/T4 | client_auth.current_user, install_guard, the two registries | T4 scopes queries by current_user; T2/T3 tests patch it to None to prove public | OK |
| T1 -> T2/T3/T4 | PUBLIC_ENDPOINTS / GATED_ENDPOINTS naming all 8 endpoints | T1's own test_registries_name_only_real_endpoints | **CONFLICT — see ruling** |
| T2 -> T3/T4 | open_store() | T3 and T4 fixtures monkeypatch client_api.open_store | OK — defined in T2, first used by T2 |
| T2 -> T4 | `request` added to the flask import line | T4 says "add from flask import request" | minor duplication, harmless |
| T4 -> none | fetch_quotes, shape_position | nothing downstream | OK |
Per-task self-consistency:
| task | finding |
| T1 | 8 tests named, plan asserts 8. The registries conflict above is the only issue. |
| T2 | 11 tests named, plan asserts 11 (after the self-review added the two bound tests). shape_call's CALL_FIELDS covers every key the tests assert. OK |
| T3 | 8 tests named, plan asserts 8. hit_rate None-not-zero and the ungraded exclusion are both directly asserted. OK |
| T4 | 15 tests named, plan asserts 15. shape_position(row, None) on create yields price_unavailable=True; no create test asserts a price, so consistent. OK |
| totals | 234 + 8 + 11 + 8 + 15 = 276, matches the plan's stated final count. OK |
Preflight finding 1 (BLOCKING, would have failed Task 1 on its own tests): Task 1 declares PUBLIC_ENDPOINTS and GATED_ENDPOINTS naming all eight endpoints, but only client_api.me exists after Task 1. Its own test_registries_name_only_real_endpoints asserts `declared - actual == set()`, which would fail with seven unmatched names. The task as written cannot pass.
Ruling: Task 1's registries contain ONLY the endpoints that exist after Task 1 — GATED_ENDPOINTS = {"client_api.me"}, PUBLIC_ENDPOINTS = frozenset(). Tasks 2, 3 and 4 each ADD their own endpoint names to the appropriate registry as part of their own task. — Why: it makes both enumeration tests pass at every commit rather than only at the end, and it makes the classification an active step each task must take rather than a list written once and trusted. The spec's requirement is that every route is classified, not that the list is written up front. — Cost if wrong: each of Tasks 2-4 has one extra line to add, and a task that forgets it fails its own suite immediately, which is the desired behaviour.
Task 1: implementer DONE (commit 98f8921, 234 -> 242). Verified live by me: signed-in /api/app/me 200; signed-out 401 with a body leaking none of sqlite/traceback/prototype//select//Users/; /api/indices 200 in BOTH states so the guard is genuinely prefix-scoped; app.py +4 -0; the single /api/app route is classified.
Task 1: review — Spec ✅, quality approved. 0 Critical, 2 Important, 2 Minor. Reviewer confirmed the things that decide later tasks: current_user is patchable at BOTH call sites (attribute access from client_api, same-module late binding inside client_auth — neither froze the name with a from-import), the guard is O(1) with no I/O and returns None on pass-through, request.endpoint being None for 404s is safe against a frozenset, and the enumeration would genuinely trip on an unregistered Task 2 endpoint.
Task 1: Ruling on both Important findings — FIX now, before Task 2 adds the first endpoint where either could matter. Both are defects in test code my brief supplied verbatim, not implementer errors. (a) _app_endpoints uses .startswith("/api/app"), a substring match, so a future /api/apply operator route would be swept into the client registry and fail the enumeration against an unrelated route. (b) test_the_operator_surface_is_untouched never patches current_user, so `current_user() is None` is always False and short-circuits the guard's `and` — the test would pass even if the guard gated every endpoint in the app. A test named for a guarantee it cannot evaluate is worse than no test. — Cost if wrong: two extra lines of test code and one tightened predicate; nothing behavioural changes.
Task 1: fix round 1/5 (3 addressed, 0 open — boundary predicate, signed-out scoping test, docstring; commits 98f8921..2b0f4f2, 244 tests). Verified: predicate captures /api/app/thing and /api/app/deep/<x> while excluding /api/apply. Implementer demonstrated the ORIGINAL operator-surface test PASSES against a deliberately broadened guard, which is the proof it could never have caught the failure it was named for. Re-review confirmed the new boundary test uses a throwaway Flask app rather than mutating the shared session fixture.
Task 1: complete (commits 717a7d7..2b0f4f2, review clean after 1 fix round)
Task 2: implementer DONE (commit f9c4c78, 244 -> 255). Both endpoints registered PUBLIC. Verified live: signed-out /api/app/calls 200 while /api/app/me stays 401, newest-first ordering, limit defaults 50 / caps 500 / falls back on garbage, sanitised 404, no banned vocabulary in the response.
Task 2: review — Spec ✅, task quality CHANGES NEEDED. 0 Critical, 1 Important, 2 Minor. Reviewer independently confirmed connection close on every path including the 404, the clamp's zero/negative behaviour, ordering determinism (ux_calls_symbol_day makes (published_at, symbol) a de facto unique sort key), and that _add_call's fixture matches the real schema with no invented columns.
Task 2: Ruling on the Important (vacuous vocabulary test) — FIX, and fix it by testing the ALLOWLIST rather than the fixture's contents. The reviewer confirmed dict(row) and shape_call(row) produce byte-identical output against today's schema, so swapping the allowlist out entirely would not fail the test; it passes because the fixture never contained a banned word. Sanitising `signal` at read time would be the wrong layer — it is free text the publish job writes, and the scorer already produces plain English. The guarantee that actually needs protecting is that shape_call emits ONLY allowlisted fields, so a column added later cannot leak. Test that by adding a column to a THROWAWAY schema and asserting it never reaches the response. — Cost if wrong: one more test that exercises a schema shape which does not exist yet in production.
Task 2: fix round 1/5 (3 addressed pending re-review — binding allowlist test, open_store leak guard, limit-edge tests; commits f9c4c78..9598874, 257 tests).
Task 2: I verified the central claim independently and it holds — with the allowlist the new test passes (rc=0), with jsonify(dict(row)) substituted it FAILS (rc=1), and the file reverted cleanly. The test genuinely binds.
Task 2: NOTE ON MY OWN METHOD — my first attempt at that experiment rewrote client_api.py and re-imported it in the SAME process, and reported "allowlist holds" for both variants. Python caches bytecode in __pycache__ keyed on source mtime at one-second granularity, so the patched run executed stale code. Had I trusted it I would have dispatched a fix round for a defect that did not exist. The reliable form is a fresh interpreter per variant with __pycache__ cleared between.
Task 2: re-review — all 3 addressed, no new breakage. Confirmed the ALTER TABLE runs against a function-scoped tmp_path database, never the production record, and that open_store's new guard closes AND re-raises rather than swallowing.
Task 2: complete (commits 2b0f4f2..9598874, review clean after 1 fix round)
Task 3: implementer DONE (commit d89ef13, 257 -> 265). Endpoint registered PUBLIC. Verified live across five scenarios: empty and nothing-resolved both give hit_rate null (serialised as literal null, not 0); 1 hit + 1 miss = 50.0; adding an ungraded row leaves it at 50.0 with resolved=2, ungraded=1; 2 hits + 1 ungraded = 100.0.
Task 3: review — Spec ✅, task quality CHANGES NEEDED. 0 Critical, 2 Important, 1 Minor. Reviewer confirmed the endpoint reads the table ONCE and aggregates in Python (so no cross-query race), the connection closes on every path, the None-not-zero test uses `is None` (the strict form that actually fails on 0.0), and the fixture writes only real columns.
Task 3: Ruling on Important 1 (banker's rounding) — FIX. round(100.0*hit/resolved, 1) is round-half-to-even, so round(6.25,1) is 6.2 while a calculator gives 6.3. I verified it: 32 divergent hit/resolved pairs at resolved<=80, smallest denominator 16. The record is empty today, so its first weeks sit at exactly those denominators — the window where a sceptical early customer is most likely to check the headline number by hand. Not a directional bias, but "our number does not match your calculator" is a bad conversation to have about a track record. — Cost if wrong: a four-line decimal import; the rate moves by 0.1 on roughly half the /16 family.
Task 3: Ruling on Important 2 (is_meaningful unguarded) — FIX. The implementation correctly counts resolved rather than total, but no test has total != resolved, so changing it to len(rows) >= 100 would pass all eight. The brief itself names this exact regression ("500 calls of which 3 are resolved must not read as meaningful") and then does not test it. — Cost if wrong: one more test.
Task 3: minor (deferred): [M1] `since` is the first call ever RECORDED, not the first resolved. A page rendering "track record since January, 62%" where the first resolved call was in June overstates the record's age. Contained because the response ships resolved/is_meaningful alongside, but nothing forces a page to use them. Ruling: defer the API change and carry the constraint into the SCREENS plan instead — the misleading rendering would happen in the template, so that is where the rule belongs.
Task 3: fix round 1/5 (2 addressed, 0 open — half-up rounding, is_meaningful coverage; commits d89ef13..1baa9a1, 268 tests). Verified all eight previously-divergent pairs now match a calculator (1/16 -> 6.3, 5/16 -> 31.3, 9/16 -> 56.3, 13/16 -> 81.3, 2/32, 10/32, 3/48, 5/80), the three null cases still return None, and 120-total/3-resolved reads not-meaningful while 120/120 does. Re-review quoted the expression: Decimal(100 * hit) / Decimal(resolved) — integer construction, so no binary float error enters before quantizing.
Task 3: complete (commits 9598874..1baa9a1, review clean after 1 fix round)
Task 4: implementer DONE (commit f3aeba1, 268 -> 284, 16 tests not 15 — the brief's prose said 15 while its own code block held 16; implementer flagged rather than silently reconciling). All four endpoints GATED. Verified live: cross-user isolation holds (someone-else sees [], PATCH on another's id 404s), a missing quote yields null/null/null with price_unavailable true and unpriced:1, all four 401 signed out, stash clean, real DB back to 0 positions.
Task 4: review — Spec ✅, task quality CHANGES NEEDED. 0 Critical, 3 Important, 3 Minor. Reviewer confirmed the two things that decide the plan: identity is read via attribute access (client_auth.current_user()) so the isolation test genuinely exercises the SQL scoping rather than passing on a no-op patch, and the PATCH whitelist is a hardcoded tuple so no user key reaches the SET clause — no injection surface.
Task 4: I confirmed all three Important findings empirically. PATCH qty=null -> 500 unhandled IntegrityError. PATCH qty="abc" -> 200, and then GET /api/app/positions -> 500 FOREVER for that user (ValueError in shape_position) — the client bricks their own book, with the damage landing on a different endpoint from the one that caused it. PATCH avg_price=0 -> pnl reported as 11000.0, the entire position value as pure profit; qty=-5 -> portfolio value -5500.0. POST rejects both with 400; PATCH accepts everything.
Task 4: Ruling on Important 1 and 2 (PATCH validation, silent quote failure) — FIX. The brief validates one door into the table and not the other, and this is the only endpoint group holding private client data. A client able to put their portfolio into a state the API refuses to create is a support ticket nobody can reproduce. — Cost if wrong: PATCH rejects some input it used to accept, which is the intent.
Task 4: Ruling on Important 3 (closed positions unreachable) — DEFER, do not fix here. positions_list filters closed_at IS NULL and no endpoint retrieves closed rows, so a closed position vanishes from the API. But the five screens in the spec do not need it: Track Record is computed from published CALLS via /api/app/record, not from the client's own closed trades, and Book shows open holdings. Adding retrieval is new scope, not a defect fix. — Why: the brief's Interfaces section speced list/create/update/delete only, and inventing a fifth endpoint inside a fix round skips the design step. — Cost if wrong: whoever later builds a "my trade history" view adds one endpoint and one test; the data is not lost, only unexposed.
Task 4: minor (deferred): [M2] test fixtures return a 2-key quote shape while kite_data.get_quotes really returns 9 keys. Harmless while shape_position reads only last_price, but the fixtures would keep passing if it were extended.
Task 4: fix round 1/5 (3 addressed, 1 NEW open — POST lacks the infinity guard PATCH now has; commits f3aeba1..3a243c9, 288 tests). Verified all nine hostile PATCH inputs now 400 with the book intact and legitimate paths working; fetch_quotes logs and still returns {}. Re-review proved partial-update semantics survive by noting the PRE-EXISTING close test still passes, which would be impossible if absent fields were defaulted.
Task 4: Ruling on the new asymmetry — FIX in round 2 rather than defer. POST got the NaN guard but not the infinity guard, so POST {"qty": Infinity} still creates a position with infinite quantity while PATCH rejects the same value. That is the exact principle this fix existed to establish — one door into the table must not accept what the other rejects — reintroduced in miniature. — Why now: one line while the implementer's context is warm, versus a whole final-review fix wave later. — Cost if wrong: POST rejects an input no legitimate client sends.
Task 4: fix round 2/5 (1 addressed, 0 open — POST infinity guard; commits 3a243c9..a5da6f1, 289 tests). POST qty=Infinity was 201 BEFORE and is 400 AFTER, so the hole was real and reachable over the wire, not theoretical. Verified both paths now agree on Infinity/-Infinity/NaN for qty and avg_price, and the legitimate create/close/render paths are untouched. Re-review confirmed _bad_number is character-for-character identical to the inline guard it replaced, so consolidating did not narrow PATCH.
Task 4: minor (deferred): [M3] the new cross-path test covers POST qty, POST avg_price and PATCH qty, but not PATCH avg_price or PATCH exit_price. The implementation guards all three through the same `numeric` tuple and the same helper, so the behaviour is correct by construction, but two of five combinations are unasserted.
Task 4: complete (commits 1baa9a1..a5da6f1, review clean after 2 fix rounds)
ALL FOUR TASKS COMPLETE. Branch 717a7d7..a5da6f1, 234 -> 289 tests.
FINAL whole-branch review (opus, 9 commits, 717a7d7..a5da6f1): CHANGES NEEDED. 0 Critical, 5 Important, 9 Minor. Confirmed no cross-client leak: ten SQL statements traced, user_id is never accepted from a body and never in PATCH's allowed tuple. Auth classification substantively correct on all eight endpoints, not merely complete. Seam clean — grepped client_api for request.headers/cookies/form/values/session/g: zero matches, all five identity reads go through client_auth.current_user().
FINAL I verified the two sharpest findings myself: opened_at as a dict or list raises sqlite3.ProgrammingError (NOT IntegrityError, so the existing handler misses it) -> unhandled 500 on a gated endpoint. And the closed-positions one-way door is real: after PATCH closed_at, positions_list no longer returns the row, and positions_list is the ONLY way to discover a pid — so a client who mis-clicks cannot obtain the id needed to reopen or delete.
FINAL Ruling — fix I1, I2, I3 (the reviewer's merge list) plus I4 and M-a in ONE wave. Skip I5 beyond a documentation note (CORS supports_credentials is pre-existing config, currently not exploitable, and changing it is Project B's decision). — Cost if wrong: five small changes instead of three.
FINAL Ruling on I2 — this is the one that would silently lose work. The ledger is gitignored AND lives in the main repo rather than this worktree, so on merge every deferred item becomes unreachable from the repository. Two of them are constraints that must bind the screens plan. They go into the spec's Deferred section, which is tracked. — Why: a constraint recorded only in a disposable workspace is a constraint nobody will ever read. — Cost if wrong: a few paragraphs in a spec that were going to be written anyway.
FINAL Ruling on the reviewer's point 4 — do NOT switch positions to half-up rounding. Its argument is right: hit_rate is a small-integer-denominator percentage the customer can recompute by hand from two integers in the same response, so a calculator mismatch is a credibility event; value/pnl come from float multiplication, are reconciled against a broker statement only approximately, and an exact .005 tie is rare. Consistency of mechanism is not worth a change with no observable benefit. — Cost if wrong: a one-paisa difference nobody checks.
FINAL fix wave: commit 32fb250, 289 -> 292 tests. All five verified by me: opened_at dict/list/int -> 400 while a valid string and omission -> 201; cross-user foreign GET 0 / PATCH 404 / DELETE 404 with the owner's qty unchanged; schema present after a fresh boot while a fresh file via open_store gets NO tables (the hoist is real); spec tracked with all four constraints; three scoped queries.
FINAL re-review: READY TO MERGE. All 5 addressed, no new breakage. Confirmed F4 fails LOUDLY — neither get_db nor init_db nor the hoisted call carries a try/except, so an unwritable database raises OperationalError at import time and crashes the boot rather than surfacing as a 500 on first request. Noted (not blocking) that the connection's immediate close relies on CPython refcounting rather than a language guarantee.
PLAN COMPLETE: 10 commits, 717a7d7..32fb250. Tests 234 -> 292.
