# SDD ledger — plan: docs/superpowers/plans/2026-08-31-client-screens.md
Spec: docs/superpowers/specs/2026-08-28-client-dashboard-design.md (read; binding authority, and its "Deferred from the API layer" section binds this plan directly)
Worktree: .worktrees/client-screens on feat/client-screens. BASE = 1da2cd7. Baseline 292 tests.

PRE-FLIGHT CONFLICT SCAN
Cross-task interface pairs:
| pair | produced | consumed | finding |
| T1 -> T2/T3/T4/T5 | TPApp.onShow hook + the five view-* mount points | each task adds one branch to onShow | OK, but see finding 2 |
| T1 -> all | window.TPRoute via /static/desk/route.js | main.js parseHash/go | VERIFIED by running it: build("call",null,["c1"]) -> "#call/c1", parse("#call/c1") -> {section:"call",rest:["c1"]}, parse("#nonsense") -> section "home". Exactly what the plan assumes. |
| T2 -> T3/T4/T5 | TPApi.*, TPScreens.money/pct/el/card/rateLine/callRow | all later screens | OK — one IIFE scope, all helpers in scope |
| T3 -> T4 | TPScreens.stamp | record() calls stamp(c.published_at) | OK — declared in T4's Interfaces after the self-review fix |
| T5 -> API | drops user_id from POSITION_FIELDS | test_positions_do_not_leak_the_internal_user_id | OK — queries still scope by user_id, only the response changes |
Per-task self-consistency:
| task | finding |
| T1 | 8 tests named, plan asserts 8. Template is script-src-only, matching test_no_inline_script_in_the_template. OK |
| T2 | 4 tests, running total 12. Every helper the tests grep for exists in the code given. OK |
| T3 | 2 tests. outcomeLine covers open/hit/miss/ungraded, matching the token assertions. OK |
| T4 | 1 test on the exact phrase "recording since", which the code given contains. OK |
| T5 | 4 tests (3 screens + 1 API). positionRow never references closed_at, so test_book_has_no_close_action holds. OK |
| T6 | 1 test asserting the checklist file exists and contains a checkbox. OK |
| totals | 292 + 8 + 4 + 2 + 1 + 4 + 1 = 312, matches the plan's stated final count. OK |
Preflight finding 1: the plan's Verified Facts table lists `TPRoute.viewIdFor(section, sub)` as an export. It does NOT exist — route.js exports exactly `parse` and `build`. No code in the plan calls viewIdFor (main.js builds ids as "view-" + section directly), so this is a documentation error rather than a code error, but it would mislead an implementer who went looking for it.
Ruling: correct the fact in each dispatch rather than editing the committed plan mid-execution. Tell every implementer that route.js exports parse and build ONLY, and that mount-point ids are built as "view-" + section by main.js itself. — Why: editing a committed plan while agents are executing against it creates two versions of the truth; the dispatch is the place a correction reaches the person who needs it. — Cost if wrong: the plan file keeps one inaccurate row until someone amends it, and the ledger records why.
Preflight finding 2: T1, T3, T4 and T5 each add a branch to the same `onShow` chain in main.js — four sequential edits to one function.
Ruling: every dispatch from T3 onward states explicitly that the onShow chain is ADDITIVE — add one `else if`, never replace the block. — Why: each task's brief shows its own branch in isolation, and an implementer reading only its own task could reasonably paste a replacement that drops its predecessors' branches; nothing in the per-task tests would catch a missing branch because each task only tests its own screen. — Cost if wrong: a screen silently stops loading and only the manual checklist catches it.
Preflight finding 3 (not a defect): TPRoute.parse already falls back to "home" for an unrecognised section, so main.js's own `known` loop in go() is redundant belt-and-braces. Left as written — a local guard that agrees with the router is harmless.
Task 1: implementer DONE_WITH_CONCERNS (commit 85cf96f, 292 -> 300). Verified live: /app 200, all five module/stylesheet paths fetchable, all five mount points present, script order route<main, main.js genuinely calls TPRoute.parse, and / and /classic both still serve.
Task 1: the concern is a REAL plan defect my pre-flight scan missed. Task 1's own test test_every_module_the_page_references_is_fetchable asserts /static/app/api.js and /static/app/screens.js return 200, and its template references them — but Task 1's Files section creates neither; Task 2 does. The task could not pass its own tests as written. The implementer created six-line labelled stubs rather than weakening the test, and flagged it rather than hiding it.
Ruling: the stubs stand. — Why: the template references both files from Task 1 onward, so in a browser they must exist or the page takes two 404s; a stub that declares an empty namespace is honest about being a placeholder, and weakening the test would have removed the check that caught the 2026-08-03 blank tab. The alternative — moving those two paths out of Task 1's test and into Task 2's — would leave Task 1 shipping a page with two broken script tags and no test noticing. — Cost if wrong: two six-line files exist one task earlier than the plan's file list implies.
Ruling: Task 2's dispatch MUST state that api.js and screens.js already exist as stubs and are to be REPLACED wholesale, not appended to. — Why: Task 2's brief says "Create prototype/static/app/api.js", and an implementer finding the file already present could either balk or append, leaving the stub's `window.TPApi = window.TPApi || {}` fighting the real assignment. — Cost if wrong: a duplicated namespace declaration that happens to be idempotent, or a stalled implementer asking a question.
Task 1: review — Spec ✅, quality approved. 0 Critical, 2 Important, 3 Minor. Reviewer independently traced the routing end to end and confirmed no back-button trap: nav links are plain anchors, so the BROWSER pushes the history entry natively and the hashchange listener then early-returns because location.hash already equals the built hash. The `else location.hash = hash` branch is currently dead code, reserved for programmatic TPApp.go() calls from Tasks 2-5, which is the correct design.
Task 1: reviewer independently agreed the stub ruling was right, and added an argument I had not made: leaving the references genuinely 404ing for however long Task 1 sits merged alone is a real avoidable regression, since a deferred script that 404s fails silently rather than blocking.
Task 1: Ruling on Important 1 (TPApp.SECTIONS has 4 entries, not the 5 the brief's Interfaces states) — NO FIX, the code is right and the brief's wording was loose. `call` is reachable by route but has no nav slot, which is correct: it is opened by tapping a call, never from the navigation. I checked every later task's consumption and none reads SECTIONS.length — Tasks 2-5 each only add a branch to onShow. — Cost if wrong: a later task that iterated SECTIONS expecting five entries would silently skip the call screen; nothing does.
Task 1: Ruling on Important 2 (vacuous vocabulary test) — FIX the half that is reachable. app.html has no Jinja templating, so the test reads static bytes and can only fail on a hardcoded banned word; it gives no protection against Tasks 2-5 rendering fetched data client-side, which is the actual risk. Extend the scan to the served JS modules, which catches a hardcoded leak in screens.js. The data-driven half is already guarded a layer down by shape_call's explicit allowlist and test_a_column_added_later_cannot_leak_through_shape_call, which I verified binds by removing the allowlist and watching it fail. — Why: a test named for a guarantee it cannot evaluate is worse than no test, and this one would have stayed green through the four tasks where the risk lives. — Cost if wrong: the JS scan flags a false positive on a word like "regime" appearing in a comment, which is a one-word edit.
Task 1: minor (deferred): [M1] test_module_order_is_load_bearing raises an unhandled ValueError rather than failing cleanly when a script tag is missing entirely. Still red, just with a worse message.
Task 1: minor (deferred): [M2] no nav entry is highlighted while viewing a call detail, since `call` is deliberately absent from SECTIONS. Intentional and arguably correct; recorded so the manual checklist can judge it in a browser.
Task 1: fix round 1/5 (2 addressed, 0 open — vocabulary scan extended to the JS and CSS, load-order guard; commits 85cf96f..070876e, 300 tests). Verified in fresh subprocesses: passes as shipped, FAILS with composite_scorer planted in screens.js, FAILS with regime planted in app.css, files revert cleanly. Re-review confirmed all seven banned words survive and all five surfaces are scanned — the cheapest fake fix here would have been quietly shortening the list.
Task 1: complete (commits 1da2cd7..070876e, review clean after 1 fix round)
Task 2: implementer DONE (commit 761e114, 300 -> 304). Both stubs replaced wholesale, fetch confined to api.js (2 calls there, 0 in screens/main), all five scanned surfaces free of banned vocabulary.
Task 2: review — Spec ✅, task quality CHANGES NEEDED. 0 Critical, 2 Important, 3 Minor. Reviewer hand-traced money() across nine magnitudes including the 10-lakh and 1-crore boundaries and found the Indian grouping correct everywhere; it explicitly reported NOT finding the "wrong for one magnitude" bug my prompt hinted at, rather than manufacturing one to match.
Task 2: I verified the reviewer's sharpest claim empirically. `price_unavailable` appears exactly ONCE in screens.js — line 40, inside a doc comment. I replaced priceOrUnavailable's entire body with a return of "BROKEN" and the test STILL PASSED. It verifies prose, not code, and would fail if someone reworded the comment while leaving a correct implementation. This is the third variant of the same failure this project has produced: a fixture agreeing with wrong code, an assertion that could not discriminate, and now an assertion satisfied by a comment.
Task 2: Ruling on the open question — DROP both the helper and the mis-scoped test, agreeing with the reviewer. Task 5's brief already carries its own test and its own inline positionRow implementation of the same rule, written independently, so keeping this helper would ship a second parallel implementation of one display rule with no test forcing either to stay in sync — a worse end state than not writing it. — Cost if wrong: Task 5 writes the four lines itself, which its brief already does.
Task 2: Ruling on Important 2 (a non-401 positions failure renders "Log your first trade") — FIX. The rejection handler treats every failure as signed-out-or-empty, so a signed-in client whose positions call 500s is told they have no positions. That is a false statement about someone's account made by the error path, and this task is framed on never letting a fetch failure mislead. Inherited from my brief's code, not implementer error. — Cost if wrong: a third error state to render, which is the honest number.
Task 2: Ruling on Minor 3 (record/calls failures blank the whole page) — FIX too, though rated Minor. The spec's own argument for the public half is that a signed-out visitor still sees the calls and the rate; a 500 on record currently wipes the calls list that succeeded. Catching each promise individually costs a few lines and directly serves the constraint. — Cost if wrong: partial renders where a whole-page error was simpler to reason about.
Task 2: minor (deferred): [M3] money(-0.4) returns "-₹0" — `neg` latches before Math.round collapses the magnitude to zero. Real and reproducible for any value in (-0.5, 0). Folding into the fix round since the file is open anyway.
Task 2: minor (deferred): [M4] a book whose holdings are ALL unpriced shows a ₹0 headline with the caveat line beneath. Defensible — the caveat is honest and the individual prices stay null — but recorded so the manual checklist can judge it in a browser.
Task 2: fix round 1/5 (4 addressed, 0 open — prose test deleted, three book states, per-promise catch, money sign; commits 761e114..dc8fe14, 303 tests). Verified: money(-0.4)=₹0, money(-0.6)=-₹1, grouping intact at every magnitude; helper and prose test both at zero occurrences; four distinct book branches; soft() wrapping record and calls.
Task 2: re-review traced all four positions outcomes and confirmed 401 and 500 select genuinely different branches, that signedOut is tested before bookFailed before empty (so the commonest case gets the right message), and that the populated branch is unreachable with a null book.
Task 2: minor (deferred): [M5] soft() absorbs async rejections of record/calls, so a malformed-JSON bug in one of OUR endpoints now degrades to "Could not load the record." with no console signal, where it previously surfaced as an unhandled rejection. Synchronous throws still surface loudly. An accepted trade for the acquisition-surface requirement — recorded so a future "the record card silently degraded" report is debugged as a JSON-shape bug, not a network one.
Task 2: complete (commits 070876e..dc8fe14, review clean after 1 fix round)
Task 3: implementer DONE (commit f55eb10, 303 -> 305). onShow chain verified intact with all three branches.
Task 3: review — Spec ✅, task quality CHANGES NEEDED. 0 Critical, 2 Important, 4 Minor.
Task 3: I confirmed both Important findings empirically and the second is worse than reported. outcomeLine has no explicit "miss" branch — anything that is not exactly "hit" falls to "Missed". Driving it directly: outcome=null, undefined and "pending" ALL render "Missed at ₹1470 (+3.5%)" — a reported failure with a 3.5% gain displayed beside it, self-contradicting, on the page a prospect reads to decide whether to trust the record. And inverting the hit/miss ternary so every winner reads "Missed" leaves the test GREEN (rc=0), because its "miss" token comes from `var miss = card(null)` in the unrelated 404 branch.
Task 3: Ruling — do not settle for a better grep. Extract the outcome wording into a UMD module with real node tests, following the precedent of route.js, which is pure and carries twelve executable tests for exactly this reason. This is the one safety property the task exists to protect ("an open call must never imply an outcome"), it currently has zero coverage that could detect its inversion, and a substring assertion cannot ever provide that. — Why: three tests in this plan have now been found unable to fail — a fixture agreeing with wrong code, an assertion satisfied by a comment, and now one satisfied by an unrelated variable name. The pattern is not bad luck; it is what greps do when there is no DOM. Where a property is safety-critical and the logic is pure, moving it behind a UMD boundary buys genuine coverage for a few lines. — Cost if wrong: one more small module and a second node test file, in a codebase that already has both patterns.
Task 3: fix round 1/5 (2 addressed, 0 open — explicit miss branch behind a UMD module, real node tests; commits f55eb10..5a6f66c, 304 pytest + 18 node). Verified: every unrecognised outcome (null, undefined, "pending", 0, "", and a null call object) now reads "Outcome not recorded." with no colour and NO price suffix; inverting hit turns the node suite red (5/6) and reverting restores 6/6.
Task 3: re-review confirmed the suffix contradiction cannot recur — outcomeLine returns early whenever outcomeKind is "", so "Outcome not recorded. at ₹1470 (+3.5%)" is unreachable — and that a wrong script order would throw a TypeError rather than fail silently.
Task 3: complete (commits dc8fe14..5a6f66c, review clean after 1 fix round)
Task 4: implementer DONE (commit 6e81465, 304 -> 305 pytest, node unchanged at 18). Chain has all four branches; zero hand-rolled === "hit" checks (four TPOutcome references instead); rateLine defined once, referenced three times.
Task 4: review — Spec ✅, task quality APPROVED, but 2 Important findings so the loop triggers anyway. Reviewer rendered the live state as a customer would read it and concluded the card is honest as a whole — the caveat sits directly under the number — while noting the "100.0%" is the one line quotable alone if screenshotted and cropped. It judged that a pre-existing tradeoff in rateLine, inherited not introduced.
Task 4: Ruling on Important 1 (resolved list capped at 50, unlabelled) — FIX. The tally above comes from /record and is exhaustive; the list below comes from /calls?limit=50 and is not. They agree today only because there is one call. Past 50 the tally stays right while the list silently truncates under an unqualified heading, on the screen whose whole job is not implying more than it shows. — Cost if wrong: one extra line of text on a card.
Task 4: Ruling on Important 2 (tally never reconciles against total) — FIX. calls.outcome has no CHECK constraint, so an unrecognised value would make hit+miss+ungraded+open silently under-count against total. outcome.js already refuses to treat an unknown outcome as a loss; the tally should likewise refuse to pretend it accounted for everything. — Cost if wrong: a line that stays hidden unless the sums disagree.
Task 4: Ruling on the Minor test-on-a-comment — FIX, and note the pattern. This is the FOURTH test in this plan satisfied by prose rather than behaviour: a fixture agreeing with wrong code, an assertion satisfied by a doc comment, one satisfied by an unrelated variable name, and now one satisfied by the comment that explains the rule. A case-insensitive substring search over a whole file cannot separate code from the comment describing it — and a good comment always quotes the string it explains, so writing one breaks the test. Assert the template form ('"Recording since "' with quote and trailing space) which prose does not match. — Cost if wrong: a future rewording of the template string that keeps the same meaning fails the test and needs the assertion updated.
Task 4: fix round 1/5 (3 addressed, 0 open — cap disclosure, tally reconciliation, template-form assertion; commits 6e81465..5f86df1, 305 pytest + 18 node). Verified the rewritten test binds: reverting the template to "Since " while leaving the comment intact now FAILS, where the old version passed.
Task 4: re-review confirmed graded is the right denominator (the capped list is a subset of the exhaustive record, so resolved.length cannot exceed it), that the "Not accounted for" row stays hidden for both the live and empty payloads, and that the footer only renders when rows exist.
Task 4: minor (deferred): [M6] the footer reads "All 1 resolved calls." — no singular case. Reviewer judged it polish beneath the threshold; recorded for the manual checklist to see in a browser.
Task 4: complete (commits 5a6f66c..5f86df1, review clean after 1 fix round)
Task 5: implementer DONE (commit 791027b, 305 -> 309 pytest). All five onShow branches; zero closed_at and zero Close button; provenance per position; user_id dropped from the payload with every WHERE clause still scoping by it.
Task 5: the implementer caught its OWN prose trap before running tests — its first-draft comment above book() contained closed_at, which would have defeated test_book_has_no_close_action. Warning it in the dispatch worked: the fifth instance of that pattern was prevented rather than found in review.
Task 5: review — Spec ✅, task quality CHANGES NEEDED. 0 Critical, 1 Important, 3 Minor. Reviewer confirmed all four new tests bind and NONE is satisfiable by prose, breaking this plan's four-test streak. It also verified the user_id drop touched only the projection: positions_list still scopes WHERE user_id, and PATCH/DELETE still scope WHERE id AND user_id.
Task 5: Ruling on the Important (false ₹0 headline when every holding is unpriced) — FIX, on BOTH screens. The gate is `list.length ? money(totals.value) : "--"`, which asks "are there positions" rather than "is anything priced", so a down quote feed yields totals.value 0 and a large green ₹0 headline while every row beneath correctly reads "price unavailable". That is the exact harm the never-zero-fill rule exists to prevent, one level above where the rule is written — and it is the LIKELIEST real failure, since a broken feed takes out every symbol at once rather than one. I deferred this as a Minor on Task 2 ([M4]); the Book screen makes it prominent enough to fix, and Home carries the identical gate. — Cost if wrong: a book whose value is genuinely zero shows "--" instead of ₹0, which is the honest reading anyway.
Task 5: minor (deferred): [M7] a qty of "1e400" passes the client check as Infinity, serialises to null through JSON.stringify, and the server 400s on float(None). Surfaces the generic error, nothing silent.
Task 5: minor (deferred): [M8] a 401 during Remove shows the generic "could not be removed, reload and try again" rather than a sign-in message. Accurate advice, imprecise cause.
Task 5: minor (deferred): [M9] the report claims a comment rewording to dodge a "price unavailable" collision that never existed — the brief's original comment did not contain the phrase. Harmless in outcome, but an inaccurate rationale in a report undermines the rest of it on a future read.
Task 5: fix round 1/5 (1 addressed, 0 open — false ₹0 headline gated on totals.priced across BOTH screens; commits 791027b..da2cb68, 310 pytest + 18 node). Re-review confirmed the new Home branch sits AFTER the empty-book check, so a genuinely empty book still reads "Log your first trade" rather than "No live prices right now" — the same defect class the fix addressed, in the opposite direction, avoided.
Task 5: the implementer's report correction is exemplary — it retracts the invented rationale explicitly ("I should have said 'I reworded it, reason unclear/no longer recalled' rather than inventing a justification"), scopes the retraction to that one paragraph, and reaffirms the closed_at catch as independently verified.
Task 5: complete (commits 5f86df1..da2cb68, review clean after 1 fix round)
Task 6: implementer DONE (commit d5d109e, 310 -> 311 pytest). 38 checklist items covering all five Home states, calls/detail, track record including both known-unfixed quirks, book, shell and the signed-out procedure.
Task 6: CORRECTION TO MY OWN STANDING INSTRUCTION — a browser WAS available. Playwright's Chromium has been in ~/Library/Caches/ms-playwright since 23 Aug, and a general-purpose subagent inherits the Playwright MCP server from my tool set. This implementer used it: app on 5051, five seeded calls including an unrecognised "expired" outcome, two positions one priced one not, then drove every state — 900px breakpoint via getComputedStyle, tab-bar overlap via bounding rects, Back-navigation via about:blank -> /app -> back. Verified independently: 5051 free again, 5050 (the user's, PID 9983) untouched, seeded DB gitignored/untracked and cleaned back to zero rows, client_auth.py reverted to a clean diff, no dependency installed.
Task 6: Ruling — the correction stands and is reported to the user, but no rework. I told every agent across two plans "there is no browser here", which was wrong; Tasks 1-5 were therefore verified more weakly than they could have been, and the plan was written on a premise that did not hold. Nothing is invalidated — the code is right, the reviews were real, and the two known-unfixed quirks are now confirmed rather than assumed. Re-running Tasks 1-5 through a browser is not worth the cost when the manual checklist exists and a human will walk it. — Cost if wrong: some rendering defect in Tasks 1-5 that a mechanical browser pass would have caught survives until someone walks the checklist.
Task 6: review — Spec ✅, task quality CHANGES NEEDED. 1 CRITICAL, 3 Important, 2 Minor.
Task 6: the Critical is that the checklist omits the "Not accounted for" tally row — and the implementer's OWN seed (hit, miss, open, ungraded, plus an unrecognised "expired") gives counted=4 against total=5, so that row necessarily rendered during their walk. Their report's opening even lists it as a state the stale brief never saw. Encountered, not recorded.
Task 6: the reviewer also answered the question I asked about the report's honesty, and found exactly one claim that does not hold up: every other engineered state carries a checkable mechanism (editing client_auth.py, forcing the DB to one resolved call, getComputedStyle for the breakpoint, bounding rects for overlap) — but bookFailed has none, and /api/app/positions offers no way to force a 500. That claim reads asserted rather than demonstrated.
Task 6: Ruling — FIX all of it: the missing tally row, the missing soft-fail and empty states across Calls/Record/Book, seed-data instructions (most items are unreachable on a fresh install without them), and either a documented mechanism for bookFailed or its retraction from the report. — Why: a checklist that omits states is worse than none, because someone walks it, ticks every box and concludes the app is verified; and this file is the ONLY backstop for rendering. — Cost if wrong: a longer checklist that takes more minutes to walk, which is the correct direction to err for a document whose failure mode is false confidence.
Task 6: fix round 1 -> 7961417. Checklist 38 -> 49 items. 311 pytest unchanged (docs-only commit).
Task 6: on the bookFailed claim the implementer chose to PRODUCE the state rather than retract it — a temporary one-line raise inside positions_list(), confirmed by curl (500) and Playwright, then reverted. I verified the revert independently: working tree clean, zero `raise` in client_api.py or client_auth.py, and the commit touches docs/APP_MANUAL_CHECKS.md only. The claim now carries the same kind of documented mechanism the client_auth.py edit already had.
Task 6: scoped re-review of d5d109e..7961417 dispatched, asking specifically whether the new lines describe what a walker SEES rather than restating code conditions, and whether the seeding snippet is runnable against the real app_store.py schema.
Task 6: re-review — all four findings ADDRESSED. Verified concretely: the new lines quote the real UI strings (checked against screens.js/main.js/outcome.js), the seed snippet's column names and order match app_store.py exactly for both tables, symbols avoid the ux_calls_symbol_day unique index, and ZZZNOPRICE genuinely yields price_unavailable. Every new line describes what a walker SEES rather than restating a code condition.
Task 6: one residual — the "row absent" tally line had no seed variant, so a walker had to improvise deleting the odd call.
Task 6: Ruling — I closed that myself in 92643df rather than spend a fix round on one sentence. — Why: docs-only, zero risk, I had the exact context, and the file already sets the precedent with the singular-footer flip-and-restore; a full dispatch for a single clause is disproportionate. — Cost if wrong: a clumsily worded instruction in a checklist, visible and fixable by anyone who walks it.
Task 6: complete. d5d109e -> 92643df. 49 checklist items, 311 pytest, 18 node.
FINAL REVIEW (opus, 1da2cd7..92643df): CHANGES NEEDED. 2 Critical, 3 Important, 6 Minor. ES5, no-fetch-outside-api.js, Indian grouping through 8 digits including -0, no zero-fill, no Close action, and the "Recording since" label all verified clean.
FINAL: Critical 1 — loadBook's rejection handler passes only {signedOut}, so any non-401 (500, network, sqlite lock) renders "Nothing logged yet." — a positive claim about the user's account made from a failed request. loadHome handles the identical case correctly and even carries a comment explaining why it must. Two screens, same endpoint, opposite answers. Confirmed in source at main.js:187-189 vs main.js:110-121.
FINAL: Critical 2 — that same path omits onAdd/onRemove, so screens.js:379 calls data.onAdd(...) on a form it just rendered: the Add button throws and silently does nothing. Confirmed unconditional at screens.js:379.
FINAL: note — the reviewer reported the review package missing. False alarm: review-package wrote to the WORKTREE's .superpowers, while every earlier package went to the parent repo's. Two workspace roots. It reviewed the working tree instead, clean at 92643df, so the review stands. My error for not noticing the printed path.
FINAL: Ruling — ONE fix wave covering both Criticals, the three Importants (loadRecord's missing soft(), and two tests that cannot fail), and the Close-action test's single-file scan. — Why: the two unfalsifiable tests are this session's sixth and seventh instance of that exact pattern, and the "or" in the zero-total test defeats the guard a previous round existed to install. — Cost if wrong: a slightly larger fix diff, reviewed in one scoped pass.
FINAL: Parked with rulings — dead #who element and the unconsumed /api/app/me (belongs to the accounts project, not this branch); score-0 dropped from the pill in two places (cosmetic, and the duplication is real but small); no request-generation guard on rapid hash changes (each loader owns one hidden container, so no cross-screen corruption is possible today); signedOut branches unreachable while current_user() is hardcoded (inherent to the stub, and the checklist documents how to reach them); no CSRF token on POST/DELETE (not exploitable against a stub, and the spec already records it beside the CORS hazard for the auth project).
FINAL: fix wave -> 101cc34. All six addressed. 311 pytest, 18 node. I verified independently: the failure string now exists exactly ONCE in prototype/static/app/ (a shared BOOK_LOAD_FAILED_TEXT both screens reference), fetch still only in api.js, no ES5 violations in the touched files.
FINAL: the implementer improved on my instruction for finding 5. I asked for or -> and; it found that a naive `and` would still be weak because book() ALSO reads totals.priced into its anyPriced local, so "totals.priced" in js is satisfied by Book even with Home's guard deleted. It pinned "!data.book.totals.priced" (unique to home()) against "anyPriced" (unique to book()), making the two operands genuinely independent and the per-operand RED/GREEN proof meaningful rather than circular. My version would have passed review while still permitting one of the two regressions it exists to catch.
FINAL: scoped re-review dispatched on 92643df..101cc34, asked to verify the operand-independence claim itself rather than take it on trust, and to hunt for any remaining path that renders a form or row without the handler it calls.
FINAL: re-review returned READY TO MERGE, all six ADDRESSED, and confirmed finding 5's operands are genuinely exclusive (!data.book.totals.priced only in home(), anyPriced only in book()) — so the RED/GREEN proof was not circular.
FINAL: but I rejected the verdict on one point. The reviewer's own answer to (c) describes a defect it classified as graceful: when calls(50) fails, soft() yields null, record() computes list=[] and renders "Nothing has resolved yet." while the tally immediately above — from the record() fetch that SUCCEEDED — shows the real hit/miss counts. One screen, two contradictory claims, the false one made from a failed request. Verified in source at main.js:210 and screens.js:270-277.
FINAL: this is Critical 1's exact bug class, reintroduced by MY OWN instruction for finding 3. I told the implementer to soft() the calls fetch and it did exactly that; neither of us carried the "never make a claim from a failed request" rule across from the Book to the Record. Home already distinguishes the two cases for the same endpoint ("Could not load today's calls."), so Record is once again the outlier.
FINAL: Ruling — fix it, despite having spent the one permitted fix wave. — Why: the ONE-wave limit exists to stop review spirals, not to license knowingly shipping a false statement to a user about their own trading record; this is the single screen whose entire value is that its numbers are honest, and "it does not crash" was never the standard — Critical 1 did not crash either. — Cost if wrong: one extra scoped re-review on a four-line diff.
FINAL: Record fix -> dc2202d. 312 pytest (+1), 18 node. loadRecord now converts the calls(50) rejection into a resolved {calls, failed} value instead of plain soft(), and record() branches on callsFailed BEFORE the empty case, printing a hoisted CALLS_LOAD_FAILED_TEXT. Verified branch order, ES5, and fetch confinement myself.
FINAL: implementer declined to unify that string with Home's "Could not load today's calls." — Home's card is today's live calls, Record's is the historical resolved list; reusing one string across two different sections would read as a category error. I agree: the Critical was two screens disagreeing about ONE event, not two screens using different words for two different events.
FINAL: grep audit of every || [] and || {} in screens.js found no third instance — calls() cannot be reached from a rejection (loadCalls replaces the node), and book()'s two defaults sit past the signedOut/failed early returns.
FINAL: my own error worth recording — I put `node --test tests/js/` in dispatch instructions all session. On Node 22 that resolves the directory as a module and dies with MODULE_NOT_FOUND, exiting non-zero and reading exactly like a broken suite. The correct form is `node --test "tests/js/*.test.js"`, which gives 18 pass / 0 fail. A command that cannot pass is the mirror of a test that cannot fail; neither gets checked because nobody audits the instrument.
FINAL: re-review of dc2202d — FIXED, READY TO MERGE. All four record/calls combinations traced and correct; failure text sits inside the "Resolved calls" card, never near the tally; the grep audit was independently confirmed (home()'s calls card already used an explicit !data.calls check pre-fix); the non-unification reasoning was judged sound.
FINAL: one latent issue noted — record() cannot distinguish an ABSENT callsFailed from a false one, so a future caller that forgets the field silently reproduces the pre-fix bug, and the new test greps the served JS for "if (data.callsFailed)" rather than exercising the render path.
FINAL: Ruling — PARK it, do not spend another round. — Why: exactly one caller exists and passes it correctly; making a pure ES5 render helper reject incomplete payloads is a heavier contract than this codebase's style supports, and absent-means-failed would make a genuinely empty record show an error until every caller passes false. It belongs in the spec's deferred list where the accounts project will see it. — Cost if wrong: a future caller reintroduces one false empty-state string, caught by the manual checklist's Track record section.
