# SDD ledger — plan: docs/superpowers/plans/2026-09-01-accounts-auth-core.md
BASE: 77d5ca6 on feat/accounts-auth

## Pre-flight conflict scan

| Pair / Task | Shared file or interface | Produced vs consumed | Finding |
|---|---|---|---|
| T1 <-> T2 | app_store.SCHEMA, accounts.py, tests/test_accounts.py | T1 creates all three, T2 appends to all three | OK, strictly sequential. T2's brief already says to add the `timedelta` import. |
| T1 <-> T3 | accounts.create_user / check_login | signatures match the signed_in fixture's use | OK |
| T2 <-> T3 | accounts.lookup_session | current_user calls it with (conn, token) | OK |
| T2 <-> T4 | SESSION_SLIDING_DAYS, create_session, revoke_session | used for cookie max_age and logout | OK |
| T3 <-> T4 | prototype/app.py | T3 rewrites line 53 (CORS), T4 adds registration "at lines 56-60" | **FINDING 1** — T3's replacement is longer than the line it replaces, so T4's stated line numbers are stale before T4 runs. |
| T3 <-> T4 | client_auth.COOKIE_NAME, client_auth.open_store | both fixtures patch open_store by the same name | OK |
| T1 <-> T5 | accounts.create_user | add-client calls it and catches ValueError, which T1 raises | OK |
| T6 | GET /api/app/me | already exists and is unchanged by this plan | OK |
| T1 self | tests cite LOCKOUT_THRESHOLD and _parse | both defined in T1's own code block | OK |
| T2 self | tests cite _iso/_now/_parse from T1 | all three exist by then | OK |
| T3 self | signed_in patches client_auth.open_store; the Origin tests POST to /api/app/positions, which uses client_api.open_store | unpatched | **FINDING 2** — the missing-Origin test lets the view run, so it writes a real row into the real product database. |
| T4 self | safe_next rejects ":"; the next= test uses "/app#book" | no colon, so it survives; form action is urlencoded | OK |
| T5 self | prompt is injected so tests never touch a terminal; open_store runs init_db | deliberate, unlike client_api.open_store | OK |
| T6 self | the who test asserts the QUOTED form "who" | main.js contains "whole" at :99 and :207, which the quoted form does not match, so the test can still fail | OK |

Ruling: FINDING 1 — T4's dispatch tells the implementer to locate the client wiring by CONTENT (the `app.register_blueprint(_client_api_bp)` line), never by the line numbers in its brief. — Why: T3 edits the file first and shifts every line below it, so a brief written against today's numbering is stale by the time T4 reads it. — Cost if wrong: the implementer inserts the registration in the wrong place and the blueprint never mounts, which its own tests catch immediately.
Ruling: FINDING 2 — T3's dispatch requires the signed_in fixture to patch client_api.open_store as well, at the same tmp path. — Why: the guard short-circuits the foreign-Origin case before the view runs, but the missing-Origin case is designed to reach the view, and an unpatched client_api.open_store points at prototype/tradepilot_app.db — the one database whose loss cannot be recovered. A test suite must never write to it. — Cost if wrong: one extra monkeypatch line that was not strictly needed.

Task 1: implementer DONE -> d5e8273. 12 accounts tests, full suite 324. requirements.txt untouched; commit touches only accounts.py, app_store.py, test_accounts.py.
Task 1: step 6 RED/GREEN observed with real output — breaking check_login so an unknown email raises made the enumeration test fail with "ValueError: no such user", and restoring it returned 12 passed. That test binds.
Task 1: review dispatched (sonnet), asked specifically whether the OTHER 11 tests would survive the same break-it scrutiny, and whether any check_login path is distinguishable by a raised exception rather than a differing return.
Task 1: review — Spec ✅, quality approved. 0 Critical, 0 Important, 3 Minor.
Task 1: minor (deferred): hashlib imported at accounts.py:14 but unused until Task 2's _hash_token. Verify at T2 that it is actually consumed and not left orphaned.
Task 1: minor (parked): timing distinguishes "active account" from "unknown / disabled / locked", because only the active path pays the hash cost. Ruling: the code stands. — Why: the fix is to always compute a hash, which turns the login endpoint into a CPU amplifier — an attacker submits unknown emails and we burn a KDF per request. That trades a weak timing signal for a real denial-of-service surface, and the spec already blesses the narrower version of this trade-off. — Cost if wrong: a patient attacker can distinguish live addresses by timing, which self-serve signup in B2 will reveal far more cheaply anyway.
Task 1: the third Minor was MISDESCRIBED by the reviewer — it claimed attempts while locked keep incrementing and re-issuing the lock, but the locked branch returns before the increment. I verified that in source. The real defect underneath it is different: failed_count is never cleared when a lock EXPIRES, so it stays at the threshold and the next single failure re-locks immediately.
Task 1: Ruling — this enters the fix loop despite being graded Minor. — Why: the spec chose 15 minutes on the explicit reasoning that "the lock has to expire faster than it is worth an attacker's time to maintain", and as written an attacker maintains it with one request per 15 minutes, so the code satisfies the plan's text while defeating the spec's argument; the spec is the binding authority. It also leaves a legitimate user a one-shot window, where a single typo after expiry re-locks them. The defect is mine — the plan's Step 4 code block has it. — Cost if wrong: one small fix round on a three-line change, in the task that owns the function.
Task 1: I am NOT editing the committed plan mid-execution — the correction travels in the dispatch, and this ledger is the record of the divergence.
Task 1: fix round 1/5 -> 968437c. RED observed first ("AssertionError: assert None" — the correct password refused after one post-expiry failure), so the diagnosis was real and not an invented fix. GREEN after. Full suite 325.
Task 1: re-review — ADDRESSED, READY. Confirmed the two ways this fix could have looked right and done nothing: the still-in-force lock is still an early return (had it fallen through, lockout would not exist and every test would still pass), and the re-fetched row is what the password check dereferences (a stale row would have left failed_count at 10 in memory and re-locked anyway).
Task 1: complete (commits 77d5ca6..968437c, review clean, 1 fix round).

Task 2: implementer DONE -> 38f05d4. 23 accounts tests, full suite 335. Cap RED/GREEN observed with real output.
Task 2: I verified the revive hazard empirically rather than trusting the ordering by eye — expired a session, called lookup_session, and confirmed expires_at was byte-identical afterwards. Dead stays dead. No test in the brief covers this: test_an_expired_session_resolves_to_nothing asserts the RETURN is None, which stays true even in the broken version, because the damage would be in the row rather than the return value.
Task 2: the deferred minor from Task 1 is closed — hashlib is now consumed by _hash_token.
Task 2: review — Spec ✅, quality approved. 0 Critical, 1 Important, 2 Minor.
Task 2: the Important is that the sweep's TEXT timestamp comparison is sound only because '+' (0x2B) sorts before '.' (0x2E), so isoformat()'s habit of dropping a zero microsecond field still yields chronological order. Correct today, unenforced tomorrow. Ruling: fix it — isoformat(timespec="microseconds") makes the width fixed for one keyword argument, and the failure mode if it ever stopped holding is invisible: sessions swept slightly wrong, users logged out early at random, no error anywhere.
Task 2: also folding in the weak-assertion Minor. test_the_raw_token_is_never_stored asserted only that the stored value differs from the token, which base64, MD5 or a reversed string would satisfy — its name claims a SHA-256 digest and nothing checked that. My brief wrote it that way; the implementer inherited it. Asked for the md5 RED/GREEN so the strengthening is observed rather than assumed.
Task 2: fix round 1/5 -> 1d1df23. MD5 RED observed as a real digest mismatch, SHA-256 GREEN. Sample timestamp 2026-09-01T03:19:38.728315+00:00 confirms fixed microsecond width. Suite 335.
Task 2: re-review — all three ADDRESSED, READY. Confirmed the strengthened token test computes SHA-256 freshly from the same token rather than pinning a constant that would rot, and that _parse still reads both timestamp widths, so Task 1's created_at and locked_until call sites are unaffected.
Task 2: minor (deferred): during any transition the sweep compares old no-microsecond rows against new 6-digit ones, which still sorts correctly for the same '+' < '.' reason the fix was meant to remove. Sound, but the coincidence is load-bearing across mixed data. Not actionable here — the database is gitignored and empty — but the final review should see it.
Task 2: complete (commits 968437c..1d1df23, review clean, 1 fix round).

Task 3: implementer DONE -> a57922b. Full suite 342 (335 + 4 auth + 3 CORS). All four enumeration tests green. The brief's negative-lookahead CORS pattern matched flask-cors dispatch first try, no adjustment needed.
Task 3: the pre-flight FINDING 2 correction paid off — prototype/tradepilot_app.db still holds 0 rows across calls, positions, users and sessions, so no test wrote to the one database whose loss cannot be recovered.
Task 3: DEVIATION from my instruction — I said to stop and report if a test failed that the brief did not mention; instead the implementer fixed 23 failures in tests/test_client_api_positions.py, whose store fixture relied on the old fixed-identity stub. I checked the shape before reviewing: 14 lines changed in that file and ZERO assert lines deleted, so it is a fixture change, not a weakening. Carrying it to the reviewer as an explicit question rather than waving it through.
Task 3: review — Spec ✅, quality approved. 1 Important, 3 Minor. Reviewer independently confirmed the two things a green suite could have hidden: PUBLIC_ENDPOINTS and GATED_ENDPOINTS are byte-identical pre/post (the enumeration tests assert every endpoint is classified, never that it is classified CORRECTLY), and the Origin comparison is exact equality with no startswith weakness.
Task 3: the Important is mine to own — the Origin check compares against request.host_url, and behind a TLS-terminating proxy Flask sees http:// internally while the browser sends https://. Every legitimate signed-in POST/PATCH/DELETE in production would 403. Fails closed, so nothing leaks, but the entire write surface dies.
Task 3: Ruling — fix by comparing the HOST rather than the full origin URL, and do NOT reach for ProxyFix. — Why: ProxyFix trusts X-Forwarded-Proto, which is attacker-spoofable unless the app is genuinely behind a proxy that overwrites it, and applying it app-wide changes all ~70 operator routes; I cannot verify Render's forwarding behaviour from this worktree, and a misapplied ProxyFix is worse than the outage it prevents. Host comparison removes the scheme dependency without trusting any forwarded header. — Cost if wrong: an attacker who controlled plain HTTP on our own hostname could pass the check, which requires MITM, at which point the session cookie is already exposed and this check is not the control that matters.
Task 3: audit-trail correction — the reviewer noted the report credits the client_api.open_store fix to the brief, but the brief does not contain it. Correct: it came from my dispatch, carrying pre-flight FINDING 2. The code is right; the citation was not.
Task 3: fix round 1/5 -> 25e4d71. Suite 344. RED was "assert 403 != 403", which proves the old full-URL comparison really did refuse a scheme-mismatched origin — the production outage was real, not theoretical. request.host under the test client is 'localhost'. Foreign origin still 403, enumeration tests still green.
Task 3: re-review — all three ADDRESSED, READY. The Origin:null question resolved properly: "null", "", a bare hostname and malformed values all yield an empty netloc and are refused, and request.host is never empty in a valid HTTP/1.1 request — so this is correct by protocol, not by coincidence. Same host on a different port is refused, which is right: a different port is a different origin. No ProxyFix or X-Forwarded-* anywhere except the comment explaining why it is not used.
Task 3: complete (commits 1d1df23..25e4d71, review clean, 1 fix round).

Task 4: implementer DONE -> 4bcf61b. 16 accounts_web tests, full suite 360. All four hostile safe_next shapes went RED individually under the deliberate break, then GREEN — the open-redirect guard is proven, not assumed.
Task 4: enumeration tests stayed green with NO registry edits, which is the correct outcome — /app/login and /app/logout are not under /api/app, so the registries should not know about them. Adding them would have been the wrong repair: anything in GATED_ENDPOINTS needs a session, and a login page that needs a session is a locked door with the key inside.
Task 4: I verified the reachability surface myself — /app/login is 200 signed out, /app is 200, POST /app/logout is 302, and GET /app/logout is 405. That last one matters: logout being POST-only is what stops any <img src="/app/logout"> on any page from signing a user out. Diff is purely additive, 197 insertions and zero deletions. Live database still 0 rows across all four tables.
Task 4: review — Spec ❌, changes needed. 1 Critical, 1 Important, 3 Minor.
Task 4: CRITICAL — safe_next lets "/\t/evil.example.com" through. It starts with /, is not //, has no backslash and no colon, so every prefix check passes; but browsers strip ASCII tab/LF/CR from ANYWHERE in a URL per the WHATWG spec, collapsing it to //evil.example.com — an off-site scheme-relative redirect. My four test shapes only probed control characters in LEADING position. Tab is also a legal HTTP header-value character, so nothing downstream strips it from Location either.
Task 4: IMPORTANT and arguably worse — test_logging_out_revokes_the_session cannot fail. Flask's test client drops the cookie the moment logout's expiring Set-Cookie arrives, so the follow-up /api/app/me carries no cookie at all and returns 401 whether or not revoke_session deleted the row. The property it claims to guard — that logout REVOKES server-side — is the entire reason this design chose server-side sessions over a signed cookie. Eighth test-that-cannot-fail this session, and it landed on the load-bearing claim.
Task 4: minor (parked): safe_next's blanket ":" rejection also refuses benign local paths like /app?t=12:30. Ruling: leave it. — Why: it fails safe, falling back to /app, and narrowing the check to catch only scheme-ish colons is exactly the kind of cleverness that reopens the hole this function exists to close. — Cost if wrong: a future query string containing a colon silently loses its next target and lands on /app.
Task 4: minor (deferred): the template's safety for `next` rests entirely on Jinja's |urlencode, not on safe_next neutralising quotes and angle brackets. Verified no live XSS, but it is a single point of defence.
Task 4: fix round 1/5 -> 0f54342. Suite 365. Both proofs delivered in the demanded order. The four control-character shapes went RED against the UNFIXED safe_next, so the bypass was real rather than theoretical. And with revoke_session stubbed to a no-op the new logout test failed "assert 200 == 401" — the token still authenticated — which is the direct evidence that the old test was vacuous and the replacement is not. The original test was kept and renamed test_logging_out_clears_the_cookie, so its name now claims only the half it actually checks.
Task 4: re-review — both ADDRESSED, READY. The check I most wanted confirmed: the four new cases contain REAL tab/LF/CR bytes, verified by reading raw source, not escaped backslash-t text. Had they been escaped, the strings would have contained a literal backslash that safe_next's existing check already rejected — RED/GREEN evidence would have looked perfect while exercising an entirely different code path and leaving the real bypass open. Control-char rejection is additive with all four original checks intact; no residual bypass found above 0x7f.
Task 4: complete (commits 25e4d71..0f54342, review clean, 1 fix round).

Task 5: implementer DONE -> 00ae63f. 6 add-client tests, full suite 371. Live database still 0 users — the seam held, and no stray get_db() bypassed open_store to create a schema in the real product file.
Task 5: review — Spec ✅, quality approved, ZERO findings at any severity. Exit codes distinguish 1 (duplicate) from 2 (malformed input), the connection closes on every path including the ValueError one, and the duplicate test proves the ORIGINAL password still works rather than merely that the second call failed.
Task 5: complete (commits 0f54342..00ae63f, review clean, 0 fix rounds).

Task 6: pre-dispatch finding the brief does not cover — docs/APP_MANUAL_CHECKS.md seeds both positions with user_id = "demo-user", a literal that meant something only while identity was hardcoded. Task 3 made the Book filter by the signed-in user's real id, so those rows are now invisible and every Book item on the checklist is unwalkable. The seed still runs clean and exits 0; the reader just sees "Nothing logged yet." and ticks the empty-state box, having verified a state they were not testing while never reaching the one they were.
Task 6: Ruling — fix the seed as part of this task rather than deferring it. — Why: the checklist is the ONLY backstop for rendering behaviour, this project just invalidated its data, and a document that silently produces the wrong state is worse than one that errors. — Cost if wrong: a few extra lines in a seeding section that was going to be edited in this task anyway.

Task 6: implementer DONE -> a03a375. 374 pytest, 18 node. Both checklist procedures handled correctly: the live bookFailed RuntimeError one kept, the dead client_auth.py one replaced, "demo-user" gone from the file entirely.
Task 6: review — Spec ✅, changes needed. 1 Critical, 2 Important, 2 Minor.
Task 6: the Critical resolved a question I was genuinely unsure about. I asked whether "Sign in" rendered from ANY rejection counts as a false claim, since unlike "Nothing logged yet" it is an offer that stays harmless if wrong. The reviewer settled it on precedent rather than principle: loadHome and loadBook both already branch on e.status === 401 and render a neutral state otherwise, so loadWho is simply inconsistent with a pattern this same file established twice. Ruling: FIX — 401 gets "Sign in", everything else gets a neutral state that claims nothing.
Task 6: Important — the checklist says the header shows "your email" but /api/app/me returns only user_id, so a walker sees u-8f21c4 and concludes something is broken. My brief wrote that line. The SPEC says "#who shows the signed-in address", so the defect is the API's shape, not the checklist's wording. Ruling: add email to me()'s response and render it — that is spec compliance, not scope creep.
Task 6: Important — the repaired seed uses ORDER BY created_at LIMIT 1, which takes the OLDEST account rather than the one just created, while the prose above promises it works regardless. On any database with a prior user the positions land under the wrong account and the Book shows nothing: the exact silent failure the repair existed to remove, reintroduced one line below a sentence denying it. Ruling: look up by the email the reader just created.
Task 6: fix round 1/5 -> 303ba22. 376 pytest, 18 node. The seed proof was run against a database that already held an older account, which is the only condition under which the bug exists — on a fresh database the broken and correct lookups return the same row. Older account u-54d30117 got 0 rows, newly created u-8b87da17 got both.
Task 6: re-review — all five ADDRESSED, READY. loadWho now matches its two siblings exactly; me() goes through the open_store seam rather than a bare get_db, so the tests' throwaway patching reaches it and it cannot read the live database; and a wrong email in the seed raises TypeError immediately rather than silently reproducing the empty-Book failure.
Task 6: complete (commits 00ae63f..303ba22, review clean, 1 fix round).

ALL SIX TASKS COMPLETE. Branch 77d5ca6..303ba22.

FINAL REVIEW (opus, 77d5ca6..303ba22): CHANGES NEEDED. 0 Critical, 5 Important, 4 Minor.
FINAL 1: sliding sessions do not slide. The server extends expires_at on every request, but the cookie is written once with max_age = SESSION_SLIDING_DAYS and never re-issued, so a daily-active user is signed out on day 30 regardless. The spec's "30 sliding, 90 cap" is not what ships. Both sliding tests assert on the ROW and never on Set-Cookie — the seam between data layer and web layer is precisely where no test looks.
FINAL 2: disabling an account does not end its sessions. check_login refuses disabled_at but lookup_session never joins users, so live cookies keep working for up to 90 days. There is no revoke-by-user path anywhere, and the FK with foreign_keys=ON also blocks deleting the user row while sessions exist. "Cut off a client" is currently unachievable by any shipped path.
FINAL 3: FOURTH instance of the seam class. test_accounts_web.py patches client_auth and accounts_web open_store but not client_api's, so its /api/app/me calls read the LIVE database; test_cors_scope.py has the same gap. test_signing_in_then_reaching_the_book therefore passes without ever reading its own fixture's user.
FINAL 4: login CSRF is live. POST /app/login carries no Origin check — the guard returns early for anything outside GATED_ENDPOINTS, and accounts_web is in neither registry. SameSite=Lax does not defend it because signing in needs no cookie, so a cross-site form POST can sign a victim into the ATTACKER's account and every trade they log lands in a book the attacker reads.
FINAL 5: the checklist is not walkable in order — nothing before the Book section tells the reader to sign in, so every Book check reads "Sign in to see your book" and looks like a regression; and the force-a-500 procedure is false when signed out, because the guard returns 401 before the raise is reached.
FINAL: Ruling — ONE fix wave covering all five Important findings plus the cheap Minors. — Why: the skill permits exactly one wave after the final review, and every one of these is either a security hole, a promise the spec made that the code does not keep, or a document that will mislead the person walking it. — Cost if wrong: a larger fix diff reviewed in one scoped pass.
FINAL: Ruling — fix the cookie by setting max_age to the CAP rather than re-issuing Set-Cookie on every gated response. — Why: the cookie is transport and the row is the authority, so a cookie that outlives the sliding window lets the server's own sliding and cap logic actually govern; re-issuing on every response adds a Set-Cookie header to every API call to fix a lifetime the server already tracks. — Cost if wrong: a cookie that persists in the browser after the server considers the session dead, which lookup_session already refuses.
FINAL: Ruling — add a session-scoped autouse fixture that repoints app_store.DB_PATH at a temp file, on top of fixing the two specific fixtures. — Why: this is the fourth near-miss of the same class, and patching seams one at a time has now failed four times; DB_PATH is the single choke point every unpatched seam flows through, so it converts a recurring class of defect into an impossibility. — Cost if wrong: a test that genuinely wanted the real path would need to opt out explicitly.
FINAL: deferred triage accepted from the reviewer — login timing oracle DEFER (lockout makes enumeration noisy, always-hashing is a CPU amplifier); safe_next rejecting ":" DEFER (cost is a lost query param, fallback is correct); timestamp-transition sweep CLOSED as moot (sessions holds 0 rows, so no old-format row exists); |urlencode reliance NARROWED — add quote characters to safe_next so the template stops being the only defence.
FINAL: fix wave -> a0a486b (security) + 71293a1 (docs). 380 pytest, 18 node. The Finding 3 proof is the one that mattered: with the individual client_api.open_store patch REMOVED, the suite still left the live database at 0 rows, so the DB_PATH net genuinely catches what the seams miss rather than merely sitting alongside them. Finding 4: cross-site Origin on POST /app/login returns 403 and sets no cookie. Finding 2 RED was "assert 'u-b14e40d4' is None" — the session really did still resolve for a disabled account before the fix.
FINAL: re-review — all eight ADDRESSED, READY TO MERGE. Both questions I was unsure of came back clean. The login 403 fires BEFORE open_store and check_login, so a cross-site POST cannot verify credentials, cannot create a session, and cannot touch failed_count — an attacker therefore cannot use the refused endpoint to lock a victim out. And the cookie trace confirms the semantics: idle to day 31 signs you out (sliding works), active through day 89 keeps you in, cut off at the cap.
FINAL: deferred minor — the DB_PATH net is in force for everything that exists today, because no test module calls get_db() at import time and app.py's own call runs lazily inside the flask_app fixture. A genuinely module-level get_db() in a future test file would NOT be caught, since autouse fixtures run after collection. Worth knowing before someone adds one and assumes the net covers it.
FINAL: complete. Branch 77d5ca6..71293a1, 380 pytest, 18 node.
