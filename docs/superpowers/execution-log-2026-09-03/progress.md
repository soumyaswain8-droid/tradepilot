# SDD ledger — plan: docs/superpowers/plans/2026-09-03-accounts-signup.md
BASE: 30d61ea on feat/accounts-signup

## Pre-flight conflict scan

| Pair / Task | Shared file or interface | Produced vs consumed | Finding |
|---|---|---|---|
| T1 <-> T2 | prototype/accounts.py | T1 appends token functions, T2 appends set_password/revoke_all_sessions | OK, different functions, strictly sequential |
| T1 <-> T2 | test files | T1 creates test_auth_tokens.py, T2 appends to the existing test_accounts.py | OK, different files |
| T1 <-> T4/T5/T6 | issue_token / peek_token / consume_token | issue_token(conn, purpose, email, hours); peek_token(conn, token) -> purpose|None; consume_token(conn, token) -> (purpose, email) | OK — every call site in T4, T5 and T6 matches these arities |
| T2 <-> T5 | set_password(conn, user_id, password), revoke_all_sessions(conn, user_id) | T5 calls both on the reset path | OK |
| T3 <-> T4 | mailer.send | T4 wraps it as accounts_web.send_mail so tests replace one function | OK |
| T3 <-> T6 | mailer.send | T6 wraps it as _default_send, injectable via main(send=) | OK |
| T4 <-> T5 | prototype/accounts_web.py, tests/test_signup_web.py | T5 APPENDS to both | **FINDING 1** — T5's brief reads like a standalone file; an implementer could replace rather than append and silently delete T4's routes. |
| T4 <-> T5 | the `store` fixture | T4 defines it patching client_auth, accounts_web and client_api open_store; T5 reuses it | OK — and the three-seam patch is right, since T5 asserts through /api/app/me which reads client_api's seam |
| T1 self | Step 4 says add `from datetime import timedelta` "if not already there" | conditional | **FINDING 2** — soft guidance. Verified: accounts.py already imports both `secrets` and `timedelta`, so the answer is "add neither". |
| T2 self | tests cite LOCKOUT_THRESHOLD, create_session, lookup_session | all exist from B1 | OK |
| T3 self | every test passes transport=; the default is only reached in production | OK — and Step 6 asks the implementer to confirm this by eye rather than infer it from a green run |
| T4 self | monkeypatches accounts_web.send_mail; the route calls the bare name | OK — Python resolves module globals at call time, so patching the attribute works |
| T5 self | the route returns 403 BEFORE consuming the token | OK, and deliberate: a CSRF attempt must not burn a victim's invite |
| T6 self | builds its own _invite_body from TRADEPILOT_URL rather than reusing accounts_web's | OK, and necessary — accounts_web builds links from request.host_url, which a CLI has no context for |

Ruling: FINDING 1 — T5's dispatch states explicitly that accounts_web.py and tests/test_signup_web.py ALREADY EXIST from T4 and must be APPENDED to, never replaced. — Why: the previous project hit exactly this shape with main.js's onShow chain, where each task's brief read like a complete specification of a file three tasks shared; nothing in T5's own tests would catch T4's routes going missing, because T5's tests only exercise set-password. — Cost if wrong: an implementer asks a clarifying question it did not need to ask.
Ruling: FINDING 2 — T1's dispatch says plainly that `secrets` and `timedelta` are both already imported and to add neither. — Why: a conditional instruction invites a duplicate import, and duplicate imports are the kind of thing a reviewer flags while the implementer defends it as following the brief. — Cost if wrong: none; the fact is verified in source.

Task 1: implementer DONE -> fe2b732. 11 token tests. Purely additive, 3 files, no duplicate imports.
Task 1: the report said "full suite: 35" — that is a subset, not the suite. I ran it myself: 391 (380 baseline + 11). A reporting error rather than a code one, but worth recording: had a schema change broken a neighbouring test, that number would have hidden it.
Task 1: RED was "assert ('invite', 'priya@example.com') == (None, None)" with the used_at clause removed — a second consume really did succeed. The implementer also stated correctly, unprompted, that this proves sequential single-use and NOT atomicity, which needs concurrency SQLite in one process will not exhibit.
Task 1: review — Spec ✅, quality approved. 0 Critical, 0 Important, 2 Minor.
Task 1: minor (deferred): peek_token and consume_token each write "used_at IS NULL AND expires_at > ?" independently. They agree today, but nothing keeps them in step — and drift in either direction is confusing: a stricter peek shows the expired page for a link that would work, a looser one shows the password form and then refuses on submit.
Task 1: the other Minor was a positive note — test_the_raw_token_is_never_stored asserts the stored value EQUALS the SHA-256 digest rather than merely differing from the token, which is the stronger claim its name implies. The previous project had to be told to strengthen exactly that assertion; this plan carried the stronger form from the start.
Task 1: complete (commits 30d61ea..fe2b732, review clean, 0 fix rounds).

Task 2: implementer DONE -> d183e43. Full suite 397 (391 + 6), reported correctly against the whole tests/ directory this time. RED was "assert None == 'u-7729480a'" — with the lockout-clearing removed, the correct password really was refused after a reset.
Task 2: review — Spec ✅, quality approved. 0 Critical, 1 Important (a carry-forward, not a defect here), 2 Minor.
Task 2: CARRY TO TASK 5 — set_password clears failed_count and locked_until for whatever user_id it is handed, with no proof of control of its own. That is correct for a data-layer primitive, but it means the lockout it clears is only safe because of WHO can call it. Task 5 must never reach set_password except after a successful consume_token, and the consume must come first. If a second caller is ever added, it inherits that obligation.
Task 2: minor (deferred): test_setting_a_password_stores_a_hash_not_the_password only asserts the literal "new password" is absent from the stored value, so it would pass for base64 or any non-literal encoding. Its name claims a hash. Same weakness the previous project had to be told to strengthen in its token test — the fix there was asserting equality against a freshly computed digest.
Task 2: minor (deferred): set_password does not check rowcount, so an unmatched user_id is a silent no-op indistinguishable from success.
Task 2: reviewer confirmed the ordering Task 5 depends on is sound — each function commits independently, and revoke_all_sessions only deletes rows existing at call time, so a session created afterwards survives. Also confirmed rowcount is reliable here because the DELETE carries a WHERE clause, so sqlite3's truncate optimisation does not apply.
Task 2: complete (commits fe2b732..d183e43, review clean, 0 fix rounds).

Task 3: implementer DONE -> e5233e7. 7 mailer tests, full suite 404. check-mail-dns.sh sidewall.in exits 0 — MX, SPF and DKIM all OK, so the gate the spec set for B2 is genuinely passing rather than assumed.
Task 3: I verified the transport seam by eye rather than trusting the green run, because a test that forgot transport= would HANG on connect to smtp.gmail.com and then fail as a network error — a signal that reads like an environment problem, not a test defect. 7 send calls, 7 carrying transport=. The only "Flask" match in mailer.py is the docstring explaining its absence.
Task 3: review — Spec ✅, quality approved with two Important test gaps. 0 Critical, 2 Important, 1 Minor.
Task 3: header injection answered properly — the reviewer read the installed stdlib and named the mechanism rather than asserting a conclusion: Message.__setitem__ routes through EmailPolicy.header_store_parse, which raises ValueError the moment a header value contains a linefeed, at assignment time and before the transport is reached. So a "to" of "victim@x.com\nBcc: attacker@evil.com" raises rather than sending. Also confirmed the password never appears in any exception this module can raise, which matters because Task 6's CLI prints exception text to a terminal.
Task 3: Important 1 — test_it_sends_from_the_configured_address asserts msg["From"] == mailer.FROM_ADDRESS, both sides of the comparison coming from one constant. It proves send() uses the variable, never that the variable is right. The SMTP endpoint test two lines later pins its literals correctly.
Task 3: Important 2 — the unconfigured guard is "if not user or not password", and nothing tests the EMPTY STRING, which is the realistic deployment failure: nobody omits SMTP_PASS from a manifest, they leave it blank or a substitution resolves to nothing. The user-only-missing case is untested too.
Task 3: minor (deferred): _smtp_transport's timeout=20 is per-operation, not an overall budget — connect, starttls, login and send can each take 20s, so a hung SMTP server could hold Task 4's synchronous Flask request for roughly 80 seconds. Carry awareness into Task 4; not fixable inside this module without restructuring.
Task 3: fix round 1/5 -> 0ee3ffb. Suite 407 (404 + 3). Both proofs delivered, and the second discriminates precisely: under an "is None" guard the run was ".FF" — the user-missing test still passed, because None IS caught by that form, while both empty-string tests failed. That is the mutation these tests exist to catch, since "be explicit about None checks" is exactly the tidy-up someone makes later.
Task 3: re-review — both ADDRESSED, READY. The reviewer independently derived the .FF pattern from the source rather than accepting the reported string: under "is None" the user-missing test passes because None IS caught, while both empty-string tests fail because "" is not None. Report and code agree.
Task 3: complete (commits d183e43..0ee3ffb, review clean, 1 fix round).

Task 4: implementer DONE -> 80883a0. 13 route tests, full suite 420 (407 + 13). All four routes present in accounts_web.py — login and logout survived, and their 8 tests pass.
Task 4: the file shows 2 deletions where a pure append would show none. I checked: both are the import lines extended in place, current_app added to the flask import and mailer to the prototype one, exactly as the dispatch instructed. No route or logic removed. Worth checking rather than assuming — the pre-flight scan flagged this file as the one an implementer could plausibly rewrite, and Task 4's own tests would not have noticed, since none of them touch login.
Task 4: RED for the identical-response test was described concretely — the known-account branch rendered the acknowledgement div while the unknown address rendered the full form, so the two bodies differed and the test caught it.
Task 4: review — Spec ✅, changes needed. 1 CRITICAL, 0 Important, 3 Minor.
Task 4: CRITICAL — /app/forgot leaks account existence through TIMING. A known address does SELECT + INSERT + a synchronous SMTP round-trip (connect, STARTTLS, login, send); an unknown one does a single indexed SELECT. Seconds versus sub-milliseconds, trivially separable over a network with averaging. The identical response body is defeated through a channel none of the 13 tests measures, because every one of them compares status and content. The reviewer notes this is prescribed by my plan verbatim, not an implementer deviation.
Task 4: Ruling — DEFER the timing fix to the deploy gate; do NOT fix it in this task. — Why: the honest fix is to stop sending inline, which means introducing a background execution model into a codebase that deliberately has none — a thread whose failures are invisible without its own logger, a daemon thread that can lose a reset email on process exit, and a second injectable seam purely to keep the tests deterministic. That is a design change, not a bug fix, and it buys nothing today: the SEBI gate already blocks this form from being publicly linked, and the spec records that the machinery is inert until someone can reach it. The spec's deploy-gate section has survived three projects being read and acted on, which is why recording it there is not the same as forgetting it. — Cost if wrong: if the form is ever linked publicly before this is addressed, a stranger can enumerate which addresses have accounts by timing the response — the exact harm the identical-body rule exists to prevent, reachable with a stopwatch.
Task 4: minor (fixing now): the except Exception also wraps reset_body(token), so a format bug in the email body would be swallowed and logged as "reset mail failed" — a programming error permanently mislabelled as a delivery failure.
Task 4: minor (fixing now): test_forgot_from_a_foreign_origin_is_refused asserts no mail was sent but never that no token row was written, unlike its signup sibling which does check the waitlist count. If the origin guard ever moved to after issue_token, the test would stay green while every probe leaked a token row.
Task 4: minor (deferred): auth_tokens has no sweep, unlike sessions. The orphan token left when a send fails is inert — its raw value existed only in the failed call's arguments and is never logged — so this is untidiness, not exposure.
Task 4: fix round 1/5 -> 2978d91. Suite 420, unchanged since no test was added, only strengthened. RED was "assert 1 == 0" on the token count with the 403 assertion still passing — the discriminating failure, proving the test catches a REORDERING that preserves the refusal, not merely a deleted guard.
Task 4: re-review — both ADDRESSED, READY. Confirmed the try now guards only send_mail, the comment still describes the right thing, and the strengthened test catches a reordering that preserves the 403.
Task 4: minor (deferred): narrowing the except leaves issue_token unguarded, so a database failure on the known-account branch would return 500 while an unknown address returned 200 — a status-code version of the same exposure. The reviewer answered a narrower question here (that the origin check protects issue_token from foreign requests, which is true but different). Ruling: leave it — a failing INSERT means the database is broken and 500 is the honest answer, and hiding it to preserve a property already conceded via timing would trade a real fault for a cosmetic one. Cost if wrong: an attacker who can induce database errors learns which addresses exist, which is a strictly harder position than simply timing the response.
Task 4: complete (commits 0ee3ffb..2978d91, review clean, 1 fix round).

Task 5: implementer DONE -> 084cf04. 22 tests in the file, full suite 429, 18 node. Both ordering proofs bind: revoke moved after create_session fails the still-signed-in test, revoke removed entirely fails the sessions-ended test. Two mutations, two different tests, neither stayed green.
Task 5: I verified the ordering in source rather than trusting it — foreign_origin, then the empty-password check, then consume_token, then create_user or set_password, then revoke_all_sessions, then create_session. set_password is reached only after a successful consume, which is the Task 2 carry-forward satisfied: the token is the proof of control that makes clearing a lockout legitimate.
Task 5: ZERO deletions in both shared files, so the pre-flight FINDING 1 hazard did not materialise — login, logout, signup and forgot all survived a task whose brief reads like a standalone specification of the file they live in.
Task 5: the report miscounted its own new tests as 8 (13 + 8 = 21, not the 22 it also reported). The file has 22 and the suite gained 9. Harmless arithmetic, but recorded because the counts are the thing I check against.
Task 5: review — Spec ✅, quality approved, but 2 Important findings both at plan level (mine), flagged for adjudication.
Task 5: Important 1 — the route consumes the token's purpose and then ignores it, branching instead on whether a users row exists. The two can disagree in both directions: an invite redeemed after the account exists takes the RESET branch and changes that account's password and revokes its sessions; a reset redeemed after the account was deleted CREATES one. The reviewer found the reachable path — test_signing_up_twice_is_accepted permits duplicate waitlist rows by design, so the same address can be approved twice, and the second invite then resets what the first created.
Task 5: Important 2 — create_user raises ValueError on the unique-email index, and the route never catches it. Two live invites for the same unregistered address, redeemed concurrently, both pass the "row is None" check and race the INSERT; the loser gets an uncaught 500 with its token already burned, since consume_token claims atomically before the race.
Task 5: Ruling — FIX both, plus the weak assertion. — Why: neither is an attack (every token involved went to one mailbox, so whoever holds them controls that address), but "an invite can silently reset an established account's password" is behaviour nobody chose — it follows from which variable the code branches on, not from a decision. Validating purpose against the branch is about six lines and converts an accident into an intention; catching the ValueError turns a 500 with a burned token into the ordinary link-no-longer-valid page. — Cost if wrong: a stale invite that would previously have let someone back into their own account now shows them an error, and they need the operator to re-approve.
Task 5: minor (fixing now): test_an_invite_link_cannot_be_used_twice asserts != 302, which would also pass on a 500 — it proves the second attempt did not succeed, not that it was refused properly.
Task 5: fix round 1/5 -> 3d0c1bb. Suite 431 (429 + 2). RED was "assert 302 == 400" — with the purpose guards removed the second invite REDIRECTED, which is the silent reset actually happening rather than merely a test failing.
Task 5: re-review — all three ADDRESSED, READY. All three load-bearing orderings traced intact. The reviewer improved on my reasoning for spend-then-reject: leaving a mismatched token unspent would keep it live until natural expiry and replayable if account state changed later — an unspent invite could match after the account is deleted and the address re-invited inside the window. Spending first keeps single-use an invariant rather than a maybe.
Task 5: minor (deferred): the except ValueError is scoped to the right statement but not the right meaning — ValueError is generic, and if create_user ever grew input validation, a real validation failure would be silently rendered as "that link is no longer valid". A dedicated exception type in accounts.py is the clean answer, and that is a contract change this task should not make unilaterally.
Task 5: complete (commits 2978d91..3d0c1bb, review clean, 1 fix round).

Task 6: implementer DONE -> 8e9a1be. 9 CLI tests, full suite 440 (431 + 9). RED for the ordering was "assert '2026-09-03T01:27:59.731566+00:00' is None" — with the UPDATE moved above the send, the row really was marked approved despite the send failing.
Task 6: the database evidence is stronger than the report stated. The worktree's copy is 0 bytes with no tables — .db is gitignored so a worktree never receives one, and the check itself created the empty file. Zero TABLES is what matters: init_db creates the whole schema on any connection through get_db, so a single test reaching that path would have left waitlist and auth_tokens behind. And the PARENT checkout, the user's real product database, holds calls/positions/users/sessions from the previous project's app boot but has NEITHER of this branch's two new tables — so this schema has never executed against the real file across 440 tests and five independent open_store seams.
ALL SIX TASKS COMPLETE. Branch 30d61ea..8e9a1be.

FINAL REVIEW (opus, 30d61ea..8e9a1be): CHANGES NEEDED. 2 Critical, 5 Important, 3 Minor.
FINAL 1 (Critical): nothing stops the suite sending REAL mail. conftest has a session-wide DB net because that seam failed four times, but this branch added a new external side effect and no equivalent net. Every route test happens to request the `sent` fixture; nothing enforces it. On a machine with SMTP credentials exported, one future test that forgets it opens a socket to Gmail and mails a real address. The spec's "nothing in the suite opens a socket" is currently a coincidence.
FINAL 2 (Critical): test_a_live_invite_renders_the_form cannot fail. It asserts b"password" is in the body, and <title>Set your password</title> sits OUTSIDE the {% if not live %} branch, so the dead-link page contains it too. peek_token could regress to always None and the test stays green — and it is the only coverage of the GET-renders-the-form path. Twelfth unfalsifiable test across the three projects this session.
FINAL 3 (Important): the spec promises "72 hours, then back to pending — the operator re-approves", and nothing ever sets approved_at back to NULL. Once approved, approve prints "is not waiting" forever. Promised and missing.
FINAL 4 (Important): two notions of the site URL — waitlist.py defaults BASE_URL to the onrender host, accounts_web builds from request.host_url. Invite and reset links from one deployment can point at different hosts.
FINAL 5 (Important): the spec asked for a test that goes red when consume_token is split into read-then-write; the shipped test is sequential, which a naive implementation passes. I acknowledged this gap at Task 1 and told the implementer to state it plainly, which it did — but the spec asked for the real thing.
FINAL 6 (Important): approve issues the token before sending, so a failed send leaves a live 72h token behind. The spec claims it changes nothing, and the test named for that invariant checks only approved_at, not the token count.
FINAL 7 (Important): completing an invite sets waitlist.user_id but never approved_at, so someone who came in via a duplicate row shows as waiting forever and approve refuses them every time.
FINAL: Ruling — ONE fix wave covering both Criticals, all five Importants, and the two triage-to-fix items. — Why: the skill permits exactly one wave; Critical 1 is a class-of-defect net rather than a bug and belongs with the others; and three of the Importants are the spec promising behaviour the code does not have, which is the category I care most about because the spec is what outlives this session. — Cost if wrong: a larger fix diff reviewed in one scoped pass.
FINAL: Ruling — attempt the concurrency test, and park it with a ruling if it cannot be made reliable rather than shipping a flaky one. — Why: a threaded SQLite race test that passes may simply not have raced, and an unreliable test on the design's central safety claim is worse than an honest note saying the property is argued rather than proven. — Cost if wrong: the atomic UPDATE stays a reasoned claim rather than a demonstrated one, which is exactly what it is today.
FINAL: fix wave -> 90584f5, 4730afd, de305cb, d78ff3b, f9adb27. Suite 447 (440 + 7), node 18. Both Critical proofs delivered: the mail net fired an AssertionError when a test was stripped of its `sent` fixture with real SMTP credentials exported, and the live-invite assertion went RED once peek_token was forced to return None.
FINAL: Important 5 came out better than I expected. Rather than reporting the concurrency test as unbuildable, it built the real race — two OS threads on independent sqlite3 connections, synchronised by a threading.Barrier, across 200 tokens — and then proved the test is not merely lucky by swapping in a naive read-then-write consume_token, which failed on the FIRST token with both threads winning. That is the discriminator: it shows the barrier produces genuine contention rather than the threads politely serialising. The atomic UPDATE is now demonstrated, not argued.
FINAL: re-review — all ten ADDRESSED, READY TO MERGE. The waitlist state enumeration came back clean: (NULL,NULL) waiting, (set,NULL) invited-not-completed, (set,set) completed, and (NULL,set) unreachable because both stamps are now written together. The concurrency test was judged trustworthy, with its residual risk being a loud BrokenBarrierError on a starved runner rather than a silent false pass.
FINAL: parked — the mail net's raise is swallowed on /app/forgot. AssertionError is an Exception subclass and send_mail sits inside that route's try/except Exception, so a forgetful test there gets a quiet 200 rather than a loud failure. Ruling: park it. — Why: the net's primary job works — no socket opens and no mail leaves the machine, which is what Critical 1 was about — and the visibility half fails only on the one route that swallows broadly; the skill permits exactly one fix wave after a final review and I have spent it, so taking a second bite here would be the rule bending to my preference. The fix is one line: raise something deriving from BaseException so a route's except Exception cannot catch it. — Cost if wrong: a future test that forgets the `sent` fixture on /app/forgot passes silently instead of failing, and the gap is discovered the next time someone reads conftest.
FINAL: complete. Branch 30d61ea..f9adb27, 447 pytest, 18 node.
